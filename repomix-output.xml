This file is a merged representation of a subset of the codebase, containing files not matching ignore patterns, combined into a single document by Repomix.

<file_summary>
This section contains a summary of this file.

<purpose>
    This file contains a packed representation of a subset of the repository's contents that is considered the most
    important context.
It is designed to be easily consumable by AI systems for analysis, code review,
or other automated processes.
</purpose>

<file_format>
The content is organized as follows:
1. This summary section
2. Repository information
3. Directory structure
4. Repository files (if enabled)
5. Multiple file entries, each consisting of:
  - File path as an attribute
  - Full contents of the file
</file_format>

<usage_guidelines>
- This file should be treated as read-only. Any changes should be made to the
  original repository files, not this packed version.
- When processing this file, use the file path to distinguish
  between different files in the repository.
- Be aware that this file may contain sensitive information. Handle it with
  the same level of security as you would the original repository.
</usage_guidelines>

<notes>
- Some files may have been excluded based on .gitignore rules and Repomix's configuration
- Binary files are not included in this packed representation. Please refer to the Repository Structure section for a complete list of file paths, including binary files
    - Files matching these patterns are excluded: android/**, ios/**, assets/**, src/i18n/translations/**,
    .agents/skills/**
- Files matching patterns in .gitignore are excluded
- Files matching default ignore patterns are excluded
- Files are sorted by Git change count (files with more changes are at the bottom)
</notes>

</file_summary>

<directory_structure>
.agents/
  rules/
    rule.mdc
  AGENTS.md
.cursor/
.gitignore
app/
  (app)/
    (tabs)/
profile/
_layout.tsx
index.tsx
      _layout.tsx
      home.tsx
      internal.tsx
      search.tsx
    details/
      [id].tsx
    _layout.tsx
    settings.tsx
  (auth)/
    _layout.tsx
    register.tsx
    sign-in.tsx
  (onboarding)/
    _layout.tsx
    welcome.tsx
_layout.tsx
index.tsx
  modal.tsx
docs/
  expo-router.md
  unistyles.md
locales/
native/
ar.json
en.json
tr.json
scripts/
  generate-svg-icons.mjs
src/
  components/
map/
index.ts
job-coordinates.ts
job-map-marker.tsx
job-map-pin.tsx
map.types.ts
MapView.tsx
    ui/
button/
button.shared.tsx
button.types.ts
chip-button.tsx
icon-button.tsx
index.tsx
tab-button.tsx
icon/
app-icon.registry.ts
index.tsx
surface/
index.ts
surface-card.tsx
typography/
index.ts
typography.tsx
      app-icon.registry.ts
      app-screen.tsx
      app-text.tsx
      index.ts
      surface-card.tsx
  core/
    fonts/
      app-fonts.ts
    navigation/
      animated-tab-bar.tsx
      paths.ts
protected-tab-layout.tsx
tab-access.ts
      tab-bar-visibility-context.tsx
use-protected-route-guard.ts
    providers/
      app-providers.tsx
login-sheet-provider.tsx
      query-provider.tsx
      theme-provider.tsx
      theme-scope.tsx
  features/
    app/
      screens/
        app-home-screen.tsx
        details-screen.tsx
        global-modal-screen.tsx
        internal-route-screen.tsx
        profile-screen.tsx
        search-screen.tsx
        settings-screen.tsx
    auth/
      hooks/
        use-session.ts
      screens/
        register-screen.tsx
        sign-in-screen.tsx
      store/
        session-store.ts
      index.ts
    home/
      screens/
        home-screen.tsx
    onboarding/
      screens/
        onboarding-welcome-screen.tsx
    public/
      screens/
        public-home-screen.tsx
  i18n/
    index.ts
    locale.ts
  services/
    api/
      api-client.ts
      index.ts
      normalize-api-error.ts
      query-client.ts
      query-keys.ts
auth/
hooks.ts
index.ts
service.ts
types.ts
jobs/
hooks.ts
index.ts
service.ts
types.ts
payment/
hooks.ts
index.ts
service.ts
types.ts
profile/
hooks.ts
index.ts
service.ts
types.ts
    storage/
      secure-storage.ts
  store/
    app-store.ts
    index.ts
    storage.ts
  theme/
    index.ts
    tokens.ts
    unistyles.ts
  types/
    svg.d.ts
.gitignore
.prettierrc.json
.svgrrc
.yarnrc.yml
app.json
babel.config.js
eslint.config.js
index.js
metro.config.js
package.json
README.md
skills-lock.json
tsconfig.json
unistyles.d.ts
</directory_structure>

<files>
This section contains the contents of the repository's files.

<file path=".cursor/.gitignore">
    plans/
</file>

<file path="app/(app)/(tabs)/profile/_layout.tsx">
    import { Stack } from 'expo-router';

    import { ProtectedTabLayout } from '@/core/navigation/protected-tab-layout';

    /** Bu sekme ve alt stack misafirlere kapali; layout duzeyinde sabit. */
    const routeAccess = 'protected' as const;

    export default function ProfileTabLayout() {
  return (
    <ProtectedTabLayout access={routeAccess}>
    <Stack screenOptions={{ headerShown: false }} />
</ProtectedTabLayout>
  );
}
</file>

<file path="app/(app)/(tabs)/profile/index.tsx">
import { ProfileScreen } from "@/features/app/screens/profile-screen";

export default function ProfileTabRoute() {
return<ProfileScreen/>;
}
</file>

<file path="app/index.tsx">
import { Redirect } from 'expo-router';

import { paths } from '@/core/navigation/paths';
import { useSession } from '@/features/auth';

/** "/" → onboarding veya ana uygulama (Stack.Protected ile uyumlu, initialRouteName gerekmez). */
export default function RootIndexRedirect() {
const { hasCompletedOnboarding, isLoading } = useSession();

if (isLoading) {
return null;
}

if (!hasCompletedOnboarding) {
return<Redirect href={paths.onboardingWelcome} />;
}

return<Redirect href={paths.appHome} />;
}
</file>

<file path="locales/native/ar.json">
{
"ios": {
"NSLocationWhenInUseUsageDescription": "يسمح لـ ae-mobil باستخدام موقعك للمحتوى القريب وميزات الخريطة.",
"NSLocationAlwaysAndWhenInUseUsageDescription": "يسمح لـ ae-mobil باستخدام موقعك للمحتوى القريب وميزات الخريطة.",
"NSLocationAlwaysUsageDescription": "يسمح لـ ae-mobil باستخدام موقعك للمحتوى القريب وميزات الخريطة."
}
}
</file>

<file path="locales/native/en.json">
{
"ios": {
"NSLocationWhenInUseUsageDescription": "Allow $(PRODUCT_NAME) to use your location.",
"NSLocationAlwaysAndWhenInUseUsageDescription": "Allow $(PRODUCT_NAME) to use your location.",
"NSLocationAlwaysUsageDescription": "Allow $(PRODUCT_NAME) to use your location."
}
}
</file>

<file path="locales/native/tr.json">
{
"ios": {
"NSLocationWhenInUseUsageDescription": "ae-mobil, konumunuzu yakın içerik ve harita özellikleri için kullanmak
istiyor.",
"NSLocationAlwaysAndWhenInUseUsageDescription": "ae-mobil, konumunuzu yakın içerik ve harita özellikleri için kullanmak
istiyor.",
"NSLocationAlwaysUsageDescription": "ae-mobil, konumunuzu yakın içerik ve harita özellikleri için kullanmak istiyor."
}
}
</file>

<file path="src/components/map/index.ts">
export { MapViewComponent as Map } from './MapView';
export type { MapViewComponentProps } from './map.types';
export { getJobLatLng } from './job-coordinates';
export { JobMapPin } from './job-map-pin';
</file>

<file path="src/components/map/job-coordinates.ts">
import type { Job } from '@/services/jobs/types';

export function getJobLatLng(job: Job): { latitude: number; longitude: number } | null {
if (Number.isFinite(job.latitude) && Number.isFinite(job.longitude)) {
return { latitude: job.latitude!, longitude: job.longitude! };
}
const loc = job.location;
if (loc && Number.isFinite(loc.lat) && Number.isFinite(loc.lng)) {
return { latitude: loc.lat, longitude: loc.lng };
}
return null;
}
</file>

<file path="src/components/map/job-map-marker.tsx">
import { Text, View } from 'react-native';
import { StyleSheet } from 'react-native-unistyles';

import type { JobStatus } from '@/services/jobs/types';
import { colors, radius, spacing, typography } from '@/theme/tokens';

function paletteForStatus(status: JobStatus) {
switch (status) {
case 'OPEN':
return { backgroundColor: colors.success, textColor: colors.neutral[0] };
case 'IN_PROGRESS':
return { backgroundColor: colors.info, textColor: colors.neutral[0] };
case 'COMPLETED':
return { backgroundColor: colors.neutral[500], textColor: colors.neutral[0] };
case 'CANCELLED':
return { backgroundColor: colors.danger, textColor: colors.neutral[0] };
default:
return { backgroundColor: colors.neutral[600], textColor: colors.neutral[0] };
}
}

type JobMapMarkerLabelProps = {
title: string;
status: JobStatus;
};

export function JobMapMarkerLabel({ title, status }: JobMapMarkerLabelProps) {
const { backgroundColor, textColor } = paletteForStatus(status);
  return (
<View style={[styles.bubble, { backgroundColor }]}>
<Text style={[styles.title, { color: textColor }]} numberOfLines={2}>
{title}
</Text>
    </View>
  );
}

        const styles = StyleSheet.create({
        bubble: {
        maxWidth: 168,
        paddingVertical: spacing.xxs,
        paddingHorizontal: spacing.xs,
        borderRadius: radius.sm,
  },
        title: {
        fontFamily: typography.fontFamily.semibold,
        fontSize: typography.fontSize.xs,
        lineHeight: typography.lineHeight.xs,
  },
        });
</file>

<file path="src/components/map/job-map-pin.tsx">
import { Text, View } from 'react-native';
import { StyleSheet } from 'react-native-unistyles';

import { Icon } from '@/components/ui/icon';
import { colors, radius, spacing, typography } from '@/theme/tokens';

type JobMapPinProps = {
category: string;
};

// Pinin toplam yüksekliğini önceden biliyoruz (İkon + Kutu + Boşluk)
// Anchor hesabının sapmaması için bu önemlidir.
const PIN_SIZE = 40;

export function JobMapPin({ category }: JobMapPinProps) {
  return (
<View style={styles.root}>
{/* İlan Kategorisi (Baloncuk) */}
<View style={styles.caption}>
<Text style={styles.captionText} numberOfLines={2}>
{category}
</Text>
        </View>

        {/* Sabit Boyutlu İkon Kapsayıcı (KAYMA ENGELLEYİCİ) */}
<View style={styles.iconContainer}>
<Icon name="marker" size={PIN_SIZE} color={colors.danger} />
        </View>
        </View>
  );
        }

        const styles = StyleSheet.create({
        root: {
        // Tüm bileşeni x ekseninde ortala
        alignItems: 'center',
        // Tüm bileşen boyutlarının net hesaplanabilmesi için genişliği serbest bırakıyoruz
        // fakat padding'leri sıfırlıyoruz.
        padding: 0,
        margin: 0,
        },
        caption: {
        maxWidth: 148,
        // İkon ile yazı arasındaki boşluk
        marginBottom: 2,
        paddingHorizontal: spacing.xs,
        paddingVertical: spacing.xxs,
        borderRadius: radius.sm,
        backgroundColor: colors.neutral[0],
        borderWidth: 1,
        borderColor: colors.app.borderSubtle,
        // Gölge (Opsiyonel ama pini haritada belirginleştirir)
        shadowColor: '#000',
        shadowOffset: { width: 0, height: 1 },
        shadowOpacity: 0.2,
        shadowRadius: 1.41,
        elevation: 2,
        },
        captionText: {
        textAlign: 'center',
        fontFamily: typography.fontFamily.semibold,
        fontSize: typography.fontSize.xs,
        lineHeight: typography.lineHeight.xs,
        color: colors.app.text,
        },
        iconContainer: {
        // İkonun boyutunu BURADA sabitliyoruz. Harita artık sadece bu kutuyu görür.
        width: PIN_SIZE,
        height: PIN_SIZE,
        justifyContent: 'center',
        alignItems: 'center',
        },
});
</file>

<file path="src/components/map/map.types.ts">
import type { GetJobsParams } from '@/services/jobs/types';

export type MapViewComponentProps = {
/** `/jobs` listesi için ek sorgu parametreleri (ör. kategori, radius). */
jobsQueryParams?: Omit<GetJobsParams, 'limit' | 'page'>;
/** Konum alınamazsa gösterilecek başlangıç bölgesi (ör. şehir merkezi). */
fallbackRegion?: {
latitude: number;
longitude: number;
latitudeDelta: number;
longitudeDelta: number;
};
};
</file>

<file path="src/components/map/MapView.tsx">
import * as Location from 'expo-location';
import * as IntentLauncher from 'expo-intent-launcher';
import { Linking, Platform, View, ActivityIndicator } from 'react-native';
import React, { useCallback, useEffect, useMemo, useRef, useState } from 'react';
import MapView, { Marker, PROVIDER_DEFAULT, type Region } from 'react-native-maps';
import { useSafeAreaInsets } from 'react-native-safe-area-context';
import { StyleSheet } from 'react-native-unistyles';

import { useJobs } from '@/services/jobs';
import { Button, Icon, IconButton, Surface, Typography } from '@/components/ui';
import { spacing } from '@/theme/tokens';

import { getJobLatLng } from './job-coordinates';
import { JobMapPin } from './job-map-pin';
import type { MapViewComponentProps } from './map.types';

// Varsayılan Başlangıç: İstanbul Merkez
const DEFAULT_FALLBACK = {
latitude: 41.0082,
longitude: 28.9784,
latitudeDelta: 0.12,
longitudeDelta: 0.12,
};

const MIN_ZOOM_DELTA = 0.002;
const MAX_ZOOM_DELTA = 80;

export function MapViewComponent({
jobsQueryParams,
fallbackRegion = DEFAULT_FALLBACK,
}: MapViewComponentProps) {
const mapRef = useRef
<React.ElementRef
<typeof MapView>>(null);
    const regionRef = useRef
    <Region>(fallbackRegion);
        const insets = useSafeAreaInsets();

        const { data: jobsResponse } = useJobs({
        limit: 100,
        ...jobsQueryParams,
        });

        // API'den gelen 0,0 veya bozuk koordinatları filtreliyoruz (Zoom out hatasını çözer)
        const jobsWithCoords = useMemo(() => {
        return (jobsResponse?.data ?? []).filter((job) => {
        const coords = getJobLatLng(job);
        if (!coords) return false;
        if (coords.latitude === 0 && coords.longitude === 0) return false;
        return true;
        });
        }, [jobsResponse]);

        const [userCoords, setUserCoords] = useState<{ latitude: number; longitude: number } | null>(
        null,
        );
        const [status, setStatus] = useState<'idle' | 'loading' | 'ready' | 'denied'>('idle');

        const requestLocationPermission = useCallback(async () => {
        setStatus('loading');
        const { status: permission } = await Location.requestForegroundPermissionsAsync();

        if (permission !== 'granted') {
        setStatus('denied');
        setUserCoords(null);
        // İzin reddedilirse İstanbul'a odaklan
        mapRef.current?.animateToRegion(fallbackRegion, 1000);
        return;
        }

        try {
        const pos = await Location.getCurrentPositionAsync({
        accuracy: Location.Accuracy.Balanced,
        });

        const coords = {
        latitude: pos.coords.latitude,
        longitude: pos.coords.longitude,
        };

        setUserCoords(coords);
        setStatus('ready');

        // Konum alındıktan sonra haritayı kullanıcının konumuna kaydır
        mapRef.current?.animateToRegion(
        {
        ...coords,
        latitudeDelta: 0.08,
        longitudeDelta: 0.08,
        },
        1000,
        );
        } catch (error) {
        console.warn('Konum alınamadı:', error);
        setStatus('denied');
        mapRef.current?.animateToRegion(fallbackRegion, 1000);
        }
        }, [fallbackRegion]);

        useEffect(() => {
        void requestLocationPermission();
        }, [requestLocationPermission]);

        // Marker'ları haritaya sığdırmak için yardımcı fonksiyon
        const fitToMarkers = useCallback(() => {
        if (!mapRef.current || jobsWithCoords.length === 0) return;

        const jobPoints = jobsWithCoords.map((j) => getJobLatLng(j)!);
        const points = [...jobPoints];

        if (userCoords) {
        points.push(userCoords);
        }

        mapRef.current.fitToCoordinates(points, {
        edgePadding: { top: 80, right: 48, bottom: 80, left: 48 },
        animated: true,
        });
        }, [jobsWithCoords, userCoords]);

        const handleOpenSettings = () => {
        if (Platform.OS === 'ios') {
        Linking.openSettings();
        } else {
        IntentLauncher.startActivityAsync(IntentLauncher.ActivityAction.LOCATION_SOURCE_SETTINGS);
        }
        };

        const clampDelta = useCallback((value: number) => {
        return Math.min(MAX_ZOOM_DELTA, Math.max(MIN_ZOOM_DELTA, value));
  }, []);

        const handleZoomIn = useCallback(() => {
        const r = regionRef.current;
        const factor = 0.55;
        const next: Region = {
        ...r,
        latitudeDelta: clampDelta(r.latitudeDelta * factor),
        longitudeDelta: clampDelta(r.longitudeDelta * factor),
        };
        mapRef.current?.animateToRegion(next, 220);
        }, [clampDelta]);

        const handleZoomOut = useCallback(() => {
        const r = regionRef.current;
        const factor = 1.75;
        const next: Region = {
        ...r,
        latitudeDelta: clampDelta(r.latitudeDelta * factor),
        longitudeDelta: clampDelta(r.longitudeDelta * factor),
        };
        mapRef.current?.animateToRegion(next, 220);
        }, [clampDelta]);

        const handleFocusMyLocation = useCallback(async () => {
        if (status === 'ready' && userCoords) {
        mapRef.current?.animateToRegion(
        {
        ...userCoords,
        latitudeDelta: 0.08,
        longitudeDelta: 0.08,
        },
        350,
        );
        return;
        }
        await requestLocationPermission();
        }, [status, userCoords, requestLocationPermission]);

  return (
        <View style={styles.container}>
        <MapView
                ref={mapRef}
        style={styles.map}
        initialRegion={fallbackRegion}
        onRegionChangeComplete={(r) => {
        regionRef.current = r;
        }}
        onMapReady={() => {
        if (jobsWithCoords.length > 0 && status !== 'loading') fitToMarkers();
        }}
        showsUserLocation={status === 'ready' && userCoords !== null}
        showsMyLocationButton={false}
        provider={PROVIDER_DEFAULT}>
        {jobsWithCoords.map((job) => {
        const coord = getJobLatLng(job)!;
        return (
        <Marker
                key={job.id}
        coordinate={coord}
        identifier={job.id}
        // x: 0.5 (Yatayda tam orta), y: 1 (Dikeyde en alt, yani kırmızı iğnenin tam sivri ucu)
        anchor={{ x: 0.5, y: 1 }}
        // Android için görselin kaymasını önleyen offset. (genellikle y: -0.5 ila -1 arası en iyi sonucu verir)
        centerOffset={{ x: 0, y: -0.5 }}
        image={require('../../../assets/icons/png/marker.png')} // Görselin yolu
        tracksViewChanges={false} // PERFORMANS: True ise sürekli re-render eder, kaymaya sebep olabilir.
        >
        <JobMapPin category={job.jobCategory} />
    </Marker>
    );
    })}
</MapView>

<View
        style={[
styles.mapControlsZoom,
{ top: insets.top + spacing.md, right: insets.right + spacing.md },
]}
pointerEvents="box-none">
<IconButton
        accessibilityLabel="Haritayı yakınlaştır"
        variant="surface"
        size="sm"
        icon={({ color, size: iconSize }) => (
<Icon name="plus" color={color} size={iconSize} />
)}
onPress={handleZoomIn}
/>
<IconButton
        accessibilityLabel="Haritayı uzaklaştır"
        variant="surface"
        size="sm"
        icon={({ color, size: iconSize }) => (
<Icon name="minus" color={color} size={iconSize} />
)}
onPress={handleZoomOut}
/>
</View>

<View
style={[
        styles.mapControlsLocate,
        { bottom: insets.bottom + spacing.md, right: insets.right + spacing.md },
        ]}
        pointerEvents="box-none">
<IconButton
accessibilityLabel="Konumuma git"
variant="surface"
size="sm"
icon={({ color, size: iconSize }) => (
<Icon name="find-geo" color={color} size={iconSize} />
        )}
        onPress={handleFocusMyLocation}
        />
        </View>

        {/* Yükleniyor Durumu */}
        {status === 'loading' && (
<View style={styles.overlay} pointerEvents="none">
<ActivityIndicator size="large"/>
        </View>
        )}

        {/* Konum İzni Reddedildiğinde Çıkan Uyarı Şeridi */}
        {status === 'denied' && (
<Surface style={styles.deniedWarning}>
<View style={styles.deniedTextContainer}>
<Typography variant="label" color="danger">
Konum İzni Gerekli
</Typography>
<Typography variant="caption" color="muted">
Size yakın ilanları ve konumunuzu görebilmek için ayarlardan izin vermelisiniz.
</Typography>
        </View>
<Button label="Ayarlar" size="sm" variant="surface" onPress={handleOpenSettings} />
        </Surface>
        )}
        </View>
        );
        }

        const styles = StyleSheet.create((theme) => ({
        container: {
        flex: 1,
        position: 'relative',
        },
        map: {
        width: '100%',
        height: '100%',
        },
        overlay: {
        ...StyleSheet.absoluteFillObject,
        alignItems: 'center',
        justifyContent: 'center',
        backgroundColor: 'rgba(255,255,255,0.45)',
        },
        deniedWarning: {
        position: 'absolute',
        top: theme.spacing.md,
        left: theme.spacing.md,
        right: theme.spacing.md,
        flexDirection: 'row',
        alignItems: 'center',
        justifyContent: 'space-between',
        padding: theme.spacing.md,
        borderRadius: theme.radius.lg,
        zIndex: 10,
        elevation: 4,
        shadowColor: theme.colors.text,
        shadowOffset: { width: 0, height: 4 },
        shadowOpacity: 0.1,
        shadowRadius: 8,
        },
        deniedTextContainer: {
        flex: 1,
        marginRight: theme.spacing.sm,
        },
        mapControlsZoom: {
        position: 'absolute',
        zIndex: 5,
        gap: theme.spacing.xs,
        alignItems: 'center',
        },
        mapControlsLocate: {
        position: 'absolute',
        zIndex: 5,
        alignItems: 'center',
        },
        }));
</file>

<file path="src/components/ui/button/button.shared.tsx">
import { ReactNode } from "react";
import {
GestureResponderEvent,
Insets,
Pressable,
PressableProps,
StyleProp,
ViewStyle,
} from "react-native";
import * as Haptics from "expo-haptics";

import type { ButtonHaptic } from "./button.types";

type InteractivePressableProps = Omit<
        PressableProps,
"children" | "onPress" | "style"
> & {
children: ReactNode;
disabledStyle?: StyleProp<ViewStyle>;
haptic?: ButtonHaptic;
onPress?: (event: GestureResponderEvent) => void;
pressedStyle?: StyleProp<ViewStyle>;
style?: StyleProp
<ViewStyle>;
    };

    export const DEFAULT_HIT_SLOP: Insets = {
    bottom: 6,
    left: 6,
    right: 6,
    top: 6,
    };

    export function InteractivePressable({
    children,
    disabled,
    disabledStyle,
    haptic,
    hitSlop = DEFAULT_HIT_SLOP,
    onPress,
    pressedStyle,
    style,
    ...props
    }: InteractivePressableProps) {
    const handlePress = (event: GestureResponderEvent) => {
    if (disabled) {
    return;
    }

    triggerHaptic(haptic);
    onPress?.(event);
    };

  return (
    <Pressable
            disabled={disabled}
    hitSlop={hitSlop}
    onPress={handlePress}
    style={({ pressed }) => [
    style,
    pressed && !disabled ? pressedStyle : undefined,
    disabled ? disabledStyle : undefined,
    ]}
    {...props}
    >
      {children}
    </Pressable>
);
}

function triggerHaptic(haptic?: ButtonHaptic) {
if (!haptic) {
return;
}

switch (haptic) {
case "light":
void Haptics.impactAsync(Haptics.ImpactFeedbackStyle.Light);
break;
case "medium":
void Haptics.impactAsync(Haptics.ImpactFeedbackStyle.Medium);
break;
case "heavy":
void Haptics.impactAsync(Haptics.ImpactFeedbackStyle.Heavy);
break;
case "selection":
void Haptics.selectionAsync();
break;
case "success":
void Haptics.notificationAsync(Haptics.NotificationFeedbackType.Success);
break;
case "warning":
void Haptics.notificationAsync(Haptics.NotificationFeedbackType.Warning);
break;
}
}

export function renderButtonIcon(
icon: ((props: { color: string; size: number }) => ReactNode) | undefined,
color: string,
size: number,
) {
if (!icon) {
return null;
}

return icon({ color, size });
}
</file>

<file path="src/components/ui/button/button.types.ts">
import { ReactNode } from "react";

export type ButtonHaptic =
| "light"
| "medium"
| "heavy"
| "selection"
| "success"
| "warning";

export type ButtonIconRenderProps = {
color: string;
size: number;
};

export type ButtonIcon = (props: ButtonIconRenderProps) => ReactNode;
</file>

<file path="src/components/ui/button/chip-button.tsx">
import { StyleProp, View, ViewStyle } from "react-native";
import { StyleSheet } from "react-native-unistyles";

import { useAppTheme } from "@/core/providers/theme-provider";

import { InteractivePressable, renderButtonIcon } from "./button.shared";
import { Typography } from "../typography";
import type { ButtonHaptic, ButtonIcon } from "./button.types";

export type ChipButtonVariant = "filter" | "category" | "tag";
export type ChipButtonSize = "sm" | "md";

type ChipButtonBaseProps = {
disabled?: boolean;
haptic?: ButtonHaptic;
onPress?: () => void;
selected?: boolean;
size?: ChipButtonSize;
style?: StyleProp
<ViewStyle>;
    trailingIcon?: ButtonIcon;
    variant?: ChipButtonVariant;
    };

    type ChipButtonProps =
    | (ChipButtonBaseProps & {
    label: string;
    leadingIcon?: ButtonIcon;
    })
    | (ChipButtonBaseProps & {
    label?: string;
    leadingIcon: ButtonIcon;
    });

    export function ChipButton({
    disabled = false,
    haptic,
    label,
    leadingIcon,
    onPress,
    selected = false,
    size = "md",
    style,
    trailingIcon,
    variant = "filter",
    }: ChipButtonProps) {
    const { theme } = useAppTheme();
    const visualTokens =
    theme.button.chip.variants[variant][selected ? "selected" : "default"];
    const iconSize = theme.button.chip.iconSize[size];

  return (
    <InteractivePressable
            accessibilityRole="button"
            accessibilityState={{ disabled, selected }}
    disabled={disabled}
    disabledStyle={styles.disabled}
    haptic={haptic}
    onPress={onPress}
    pressedStyle={styles.pressed}
    style={[
    styles.base,
    {
    backgroundColor: visualTokens.backgroundColor,
    borderColor: visualTokens.borderColor,
    borderWidth: visualTokens.borderWidth,
    minHeight: size === "md" ? 36 : 30,
    paddingHorizontal: size === "md" ? theme.spacing.md : theme.spacing.sm,
    paddingVertical: theme.spacing.xs,
    },
    style,
    ]}
    >
    <View style={styles.content}>
    {renderButtonIcon(leadingIcon, visualTokens.iconColor, iconSize)}
    {label ? (
    <Typography
            style={[styles.label, { color: visualTokens.textColor }]}
    variant={size === "md" ? "buttonSmall" : "caption"}
    >
    {label}
</Typography>
) : null}
{renderButtonIcon(trailingIcon, visualTokens.iconColor, iconSize)}
</View>
        </InteractivePressable>
        );
}

        const styles = StyleSheet.create((theme) => ({
        base: {
        alignItems: "center",
        borderRadius: theme.button.chip.radius,
        justifyContent: "center",
        },
        content: {
        alignItems: "center",
        flexDirection: "row",
        gap: theme.spacing.xs,
        justifyContent: "center",
        },
        disabled: {
        opacity: theme.button.common.disabledOpacity,
        },
        label: {
        fontWeight: "500",
        },
        pressed: {
        opacity: theme.button.common.pressedOpacity,
        },
        }));
</file>

<file path="src/components/ui/button/icon-button.tsx">
import { StyleProp, ViewStyle } from "react-native";
import { StyleSheet } from "react-native-unistyles";

import { useAppTheme } from "@/core/providers/theme-provider";

import { InteractivePressable, renderButtonIcon } from "./button.shared";
import type { ButtonHaptic, ButtonIcon } from "./button.types";

export type IconButtonVariant = "ghost" | "surface" | "primary" | "destructive";
export type IconButtonSize = "sm" | "md" | "lg";

type IconButtonProps = {
accessibilityLabel: string;
disabled?: boolean;
haptic?: ButtonHaptic;
icon: ButtonIcon;
onPress?: () => void;
selected?: boolean;
size?: IconButtonSize;
style?: StyleProp
<ViewStyle>;
    variant?: IconButtonVariant;
    };

    export function IconButton({
    accessibilityLabel,
    disabled = false,
    haptic,
    icon,
    onPress,
    selected = false,
    size = "md",
    style,
    variant = "ghost",
    }: IconButtonProps) {
    const { theme } = useAppTheme();
    const sizeTokens = theme.button.icon.sizes[size];
    const variantTokens = selected
    ? theme.button.icon.selected
    : theme.button.icon.variants[variant];
    const iconColor = disabled
    ? theme.button.disabled.iconColor
    : variantTokens.iconColor;

    return (
    <InteractivePressable
            accessibilityLabel={accessibilityLabel}
    accessibilityRole="button"
    disabled={disabled}
    disabledStyle={styles.disabled}
    haptic={haptic}
    onPress={onPress}
    pressedStyle={styles.pressed}
    style={[
    styles.base,
    {
    backgroundColor: variantTokens.backgroundColor,
    borderColor: variantTokens.borderColor,
    borderWidth: variantTokens.borderWidth,
    height: sizeTokens.size,
    width: sizeTokens.size,
    },
    style,
    ]}
    >
    {renderButtonIcon(icon, iconColor, sizeTokens.iconSize)}
</InteractivePressable>
);
}

const styles = StyleSheet.create((theme) => ({
base: {
alignItems: "center",
borderRadius: theme.button.icon.radius,
justifyContent: "center",
},
disabled: {
opacity: theme.button.common.disabledOpacity,
},
pressed: {
opacity: theme.button.common.pressedOpacity,
},
}));
</file>

<file path="src/components/ui/button/index.tsx">
import { ActivityIndicator, StyleProp, View, ViewStyle } from "react-native";
import { StyleSheet } from "react-native-unistyles";

import { useAppTheme } from "@/core/providers/theme-provider";

import { Typography } from "../typography";
import { InteractivePressable, renderButtonIcon } from "./button.shared";
import type { ButtonHaptic, ButtonIcon } from "./button.types";

export type ButtonVariant =
| "primary"
| "secondary"
| "surface"
| "ghost"
| "destructive"
| "positive";
export type ButtonSize = "sm" | "md" | "lg";

type ButtonProps = {
disabled?: boolean;
fullWidth?: boolean;
haptic?: ButtonHaptic;
label: string;
leadingIcon?: ButtonIcon;
loading?: boolean;
onPress?: () => void;
size?: ButtonSize;
style?: StyleProp
<ViewStyle>;
    trailingIcon?: ButtonIcon;
    variant?: ButtonVariant;
    };

    export function Button({
    disabled = false,
    fullWidth = false,
    haptic,
    label,
    leadingIcon,
    loading = false,
    onPress,
    size = "md",
    style,
    trailingIcon,
    variant = "primary",
    }: ButtonProps) {
    const { theme } = useAppTheme();
    const isDisabled = disabled || loading;
    const sizeTokens = theme.button.cta.sizes[size];
    const variantTokens = theme.button.cta.variants[variant];
    const disabledTokens = theme.button.disabled;
    const visualTokens = isDisabled ? disabledTokens : variantTokens;

  return (
    <InteractivePressable
            accessibilityRole="button"
            disabled={isDisabled}
    disabledStyle={styles.disabled}
    haptic={haptic}
    onPress={onPress}
    pressedStyle={styles.pressed}
    style={[
    styles.base,
    styles[fullWidth ? "fullWidth" : "intrinsicWidth"],
    {
    backgroundColor: visualTokens.backgroundColor,
    borderColor: visualTokens.borderColor,
    borderWidth: visualTokens.borderWidth,
    minHeight: sizeTokens.minHeight,
    paddingHorizontal: sizeTokens.paddingHorizontal,
    paddingVertical: sizeTokens.paddingVertical,
    },
    style,
    ]}
    >
    <View style={[styles.content, { gap: sizeTokens.gap }]}>
    {loading ? (
    <ActivityIndicator color={visualTokens.textColor} size="small" />
    ) : (
    renderButtonIcon(leadingIcon, visualTokens.iconColor, sizeTokens.iconSize)
    )}
    <Typography
            color="default"
            style={[styles.label, { color: visualTokens.textColor }]}
    variant={getTypographyVariant(size)}
    >
    {label}
</Typography>
{!loading
? renderButtonIcon(trailingIcon, visualTokens.iconColor, sizeTokens.iconSize)
: null}
</View>
        </InteractivePressable>
        );
}

        function getTypographyVariant(size: ButtonSize) {
        switch (size) {
        case "sm":
        return "buttonSmall";
        case "lg":
        return "buttonLarge";
        case "md":
        default:
        return "button";
        }
        }

        const styles = StyleSheet.create((theme) => ({
        base: {
        alignItems: "center",
        borderRadius: theme.button.cta.radius,
        justifyContent: "center",
        },
        content: {
        alignItems: "center",
        flexDirection: "row",
        justifyContent: "center",
        },
        disabled: {
        opacity: theme.button.common.disabledOpacity,
        },
        fullWidth: {
        alignSelf: "stretch",
        },
        intrinsicWidth: {
        alignSelf: "flex-start",
        },
        label: {
        fontWeight: "500",
        },
        pressed: {
        opacity: theme.button.common.pressedOpacity,
        },
        }));

        export { ChipButton } from './chip-button';
        export { IconButton } from './icon-button';
        export { TabButton } from './tab-button';
        export type { ChipButtonVariant, ChipButtonSize } from './chip-button';
        export type { IconButtonVariant, IconButtonSize } from './icon-button';
        export type { TabButtonSize } from './tab-button';
        export type { ButtonHaptic, ButtonIcon } from './button.types';
</file>

<file path="src/components/ui/button/tab-button.tsx">
import { StyleProp, View, ViewStyle } from "react-native";
import { StyleSheet } from "react-native-unistyles";

import { useAppTheme } from "@/core/providers/theme-provider";

import { Typography } from "../typography";
import { InteractivePressable, renderButtonIcon } from "./button.shared";
import type { ButtonHaptic, ButtonIcon } from "./button.types";

export type TabButtonSize = "sm" | "md";

type TabButtonProps = {
badge?: number | string;
disabled?: boolean;
haptic?: ButtonHaptic;
icon?: ButtonIcon;
label: string;
onPress?: () => void;
selected?: boolean;
size?: TabButtonSize;
style?: StyleProp
<ViewStyle>;
    };

    export function TabButton({
    badge,
    disabled = false,
    haptic,
    icon,
    label,
    onPress,
    selected = false,
    size = "md",
    style,
    }: TabButtonProps) {
    const { theme } = useAppTheme();
    const visualTokens =
    theme.button.tab.variants[selected ? "selected" : "default"];
    const sizeTokens = theme.button.tab.sizes[size];

  return (
    <InteractivePressable
            accessibilityRole="tab"
            accessibilityState={{ disabled, selected }}
    disabled={disabled}
    disabledStyle={styles.disabled}
    haptic={haptic}
    onPress={onPress}
    pressedStyle={styles.pressed}
    style={[
    styles.base,
    {
    backgroundColor: visualTokens.backgroundColor,
    borderColor: visualTokens.borderColor,
    borderWidth: visualTokens.borderWidth,
    minHeight: sizeTokens.minHeight,
    paddingHorizontal: sizeTokens.paddingHorizontal,
    paddingVertical: sizeTokens.paddingVertical,
    },
    style,
    ]}
    >
    <View style={styles.content}>
    {renderButtonIcon(icon, visualTokens.iconColor, sizeTokens.iconSize)}
    <Typography
            style={[styles.label, { color: visualTokens.textColor }]}
    variant="buttonSmall"
    >
    {label}
</Typography>
{badge !== undefined ? (
<View style={styles.badge}>
<Typography color="dangerForeground" variant="caption">
    {badge}
</Typography>
</View>
        ) : null}
        </View>
        </InteractivePressable>
        );
}

        const styles = StyleSheet.create((theme) => ({
        badge: {
        alignItems: "center",
        backgroundColor: theme.button.tab.badge.backgroundColor,
        borderRadius: theme.radius.full,
        justifyContent: "center",
        minWidth: 20,
        paddingHorizontal: theme.spacing.xxs,
        },
        base: {
        alignItems: "center",
        borderRadius: theme.button.tab.radius,
        justifyContent: "center",
        },
        content: {
        alignItems: "center",
        flexDirection: "row",
        gap: theme.spacing.xs,
        justifyContent: "center",
        },
        disabled: {
        opacity: theme.button.common.disabledOpacity,
        },
        label: {
        fontWeight: "500",
        },
        pressed: {
        opacity: theme.button.common.pressedOpacity,
        },
        }));
</file>

<file path="src/components/ui/icon/app-icon.registry.ts">
// This file is auto-generated by `npm run icons:generate`.
// Do not edit it manually.
import IconAdd1Svg from "../../../../assets/icons/svg/add-1.svg";
import IconFindGeoSvg from "../../../../assets/icons/svg/find-geo.svg";
import IconMarkerSvg from "../../../../assets/icons/svg/marker.svg";
import IconMinusSvg from "../../../../assets/icons/svg/minus.svg";
import IconPlusSvg from "../../../../assets/icons/svg/plus.svg";

export const appIcons = {
"add-1": IconAdd1Svg,
"find-geo": IconFindGeoSvg,
"marker": IconMarkerSvg,
"minus": IconMinusSvg,
"plus": IconPlusSvg,
} as const;

export type AppIconName = keyof typeof appIcons;
</file>

<file path="src/components/ui/icon/index.tsx">
import { memo, type ComponentType } from 'react';
import type { SvgProps } from 'react-native-svg';

import { appIcons, type AppIconName } from './app-icon.registry';

export type AppIconProps = Omit<SvgProps, 'width' | 'height'> & {
name: AppIconName;
size?: number;
width?: number | string;
height?: number | string;
};

type SvgIconComponent = ComponentType<SvgProps>;

export const Icon = memo(function IconComponent({
  name,
size = 24,
width,
height,
color,
...props
}: AppIconProps) {
const Svg = appIcons[name] as SvgIconComponent;

return<Svg color={color} height={height ?? size} width={width ?? size} {...props} />;
});

export type { AppIconName };
</file>

<file path="src/components/ui/surface/index.ts">
export { Surface } from './surface-card';
</file>

<file path="src/components/ui/surface/surface-card.tsx">
import { PropsWithChildren } from 'react';
import { StyleProp, View, ViewStyle } from 'react-native';
import { StyleSheet } from 'react-native-unistyles';

type SurfaceProps = PropsWithChildren<{
style?: StyleProp
<ViewStyle>;
    }>;

    export function Surface({ children, style }: SurfaceProps) {
    return<View style={[styles.root, style]}>{children}
</View>
;
}

const styles = StyleSheet.create((theme) => ({
root: {
backgroundColor: theme.colors.surface,
borderColor: theme.colors.border,
borderRadius: theme.radius.lg,
borderWidth: 1,
gap: theme.spacing.sm,
padding: theme.spacing.lg,
},
}));
</file>

<file path="src/components/ui/typography/index.ts">
export { Typography } from './typography';
</file>

<file path="src/components/ui/typography/typography.tsx">
import { ReactNode } from 'react';
import { Platform, StyleProp, Text, TextStyle } from 'react-native';
import { StyleSheet } from 'react-native-unistyles';

import { useLocaleSettings } from '@/i18n';

type TypographyVariant =
| 'body'
| 'bodyMuted'
| 'button'
| 'buttonLarge'
| 'buttonSmall'
| 'caption'
| 'headline'
| 'label'
| 'title';

type TypographyColor =
| 'danger'
| 'default'
| 'dangerForeground'
| 'info'
| 'muted'
| 'positive'
| 'primary'
| 'primaryForeground'
| 'success'
| 'warning';

type TypographyProps = {
children: ReactNode;
color?: TypographyColor;
style?: StyleProp
<TextStyle>;
    variant?: TypographyVariant;
    };

    export function Typography({
    children,
    color = 'default',
    style,
    variant = 'body',
    }: TypographyProps) {
    const { direction, isRTL, locale } = useLocaleSettings();
    const webDirectionProps =
    Platform.OS === 'web' ? { dir: direction, lang: locale } : undefined;

  return (
    <Text
            style={[
    styles[variant],
    styles[color],
    isRTL ? styles.alignRight : styles.alignLeft,
    style,
    ]}
    {...webDirectionProps}
    >
    {children}
</Text>
);
}

const styles = StyleSheet.create((theme) => ({
alignLeft: {
textAlign: 'left',
},
alignRight: {
textAlign: 'right',
},
body: {
color: theme.colors.text,
fontFamily: theme.typography.fontFamily.regular,
fontSize: theme.typography.fontSize.md,
lineHeight: theme.typography.lineHeight.md,
},
bodyMuted: {
color: theme.colors.textMuted,
fontFamily: theme.typography.fontFamily.regular,
fontSize: theme.typography.fontSize.md,
lineHeight: theme.typography.lineHeight.md,
},
button: {
color: theme.colors.text,
fontFamily: theme.typography.fontFamily.medium,
fontSize: theme.typography.fontSize.md,
fontWeight: '500',
lineHeight: theme.typography.lineHeight.md,
},
buttonLarge: {
color: theme.colors.text,
fontFamily: theme.typography.fontFamily.medium,
fontSize: theme.typography.fontSize.lg,
fontWeight: '500',
lineHeight: theme.typography.lineHeight.lg,
},
buttonSmall: {
color: theme.colors.text,
fontFamily: theme.typography.fontFamily.medium,
fontSize: theme.typography.fontSize.sm,
fontWeight: '500',
lineHeight: theme.typography.lineHeight.sm,
},
caption: {
color: theme.colors.textMuted,
fontFamily: theme.typography.fontFamily.regular,
fontSize: theme.typography.fontSize.xs,
lineHeight: theme.typography.lineHeight.xs,
},
danger: {
color: theme.colors.danger,
},
dangerForeground: {
color: theme.colors.dangerForeground,
},
default: {},
headline: {
color: theme.colors.text,
fontFamily: theme.typography.fontFamily.semibold,
fontSize: theme.typography.fontSize.lg,
fontWeight: '600',
lineHeight: theme.typography.lineHeight.lg,
},
info: {
color: theme.colors.info,
},
label: {
color: theme.colors.primary,
fontFamily: theme.typography.fontFamily.semibold,
fontSize: theme.typography.fontSize.sm,
fontWeight: '600',
letterSpacing: 0.4,
lineHeight: theme.typography.lineHeight.sm,
textTransform: 'uppercase',
},
muted: {
color: theme.colors.textMuted,
},
positive: {
color: theme.colors.positive,
},
primary: {
color: theme.colors.primary,
},
primaryForeground: {
color: theme.colors.primaryForeground,
},
success: {
color: theme.colors.success,
},
title: {
color: theme.colors.text,
fontFamily: theme.typography.fontFamily.bold,
fontSize: theme.typography.fontSize.xxl,
fontWeight: '700',
lineHeight: theme.typography.lineHeight.xxl,
},
warning: {
color: theme.colors.warning,
},
}));
</file>

<file path="src/core/navigation/protected-tab-layout.tsx">
import { useFocusEffect } from '@react-navigation/native';
import { useRouter } from 'expo-router';
import type { PropsWithChildren } from 'react';
import { useCallback, useEffect, useRef } from 'react';

import { useLoginSheet } from '@/core/providers/login-sheet-provider';
import { useSession } from '@/features/auth';

import { paths } from './paths';
import type { TabAccess } from './tab-access';

type ProtectedTabLayoutProps = PropsWithChildren<{
readonly access: TabAccess;
}>;

/**
* Korumali sekme alt agacinda misafir + deep link / cikis senaryolarini yonetir.
* `access: "public"` ise sadece children render edilir.
*/
export function ProtectedTabLayout({ access, children }: ProtectedTabLayoutProps) {
const { isAuthenticated } = useSession();
const router = useRouter();
const { presentLoginSheet } = useLoginSheet();
const wasAuthenticatedRef = useRef(isAuthenticated);

useEffect(() => {
if (access !== 'protected') {
return;
}
if (wasAuthenticatedRef.current && !isAuthenticated) {
router.replace(paths.appHome);
presentLoginSheet();
}
wasAuthenticatedRef.current = isAuthenticated;
}, [access, isAuthenticated, presentLoginSheet, router]);

useFocusEffect(
useCallback(() => {
if (access !== 'protected') {
return;
}
if (!isAuthenticated) {
router.replace(paths.appHome);
presentLoginSheet();
}
}, [access, isAuthenticated, presentLoginSheet, router]),
);

if (access === 'protected' && !isAuthenticated) {
return null;
}

return children;
}
</file>

<file path="src/core/navigation/tab-access.ts">
export type TabAccess = "public" | "protected";

/**
* Tab route adlari (Expo Router `Tabs.Screen name` ile eslesmeli).
*/
export const tabAccess: Record<string, TabAccess> = {
home: "public",
search: "public",
profile: "protected",
internal: "protected",
};

export function getTabAccess(screenName: string): TabAccess {
return tabAccess[screenName] ?? "protected";
}
</file>

<file path="src/core/navigation/use-protected-route-guard.ts">
import { useFocusEffect } from "@react-navigation/native";
import { useRouter } from "expo-router";
import { useCallback, useEffect, useRef } from "react";

import { useLoginSheet } from "@/core/providers/login-sheet-provider";
import { useSession } from "@/features/auth";

import { paths } from "./paths";

/**
* Tek ekranlik (klasorsuz) korumali tab rotalari icin — or. `href: null` internal sekme.
*/
export function useProtectedRouteGuard() {
const { isAuthenticated } = useSession();
const router = useRouter();
const { presentLoginSheet } = useLoginSheet();
const wasAuthenticatedRef = useRef(isAuthenticated);

  useEffect(() => {
if (wasAuthenticatedRef.current && !isAuthenticated) {
router.replace(paths.appHome);
presentLoginSheet();
}
wasAuthenticatedRef.current = isAuthenticated;
}, [isAuthenticated, presentLoginSheet, router]);

useFocusEffect(
useCallback(() => {
if (!isAuthenticated) {
router.replace(paths.appHome);
presentLoginSheet();
}
}, [isAuthenticated, presentLoginSheet, router]),
);

return isAuthenticated;
}
</file>

<file path="src/core/providers/login-sheet-provider.tsx">
import {
BottomSheetBackdrop,
BottomSheetModal,
BottomSheetModalProvider,
BottomSheetScrollView,
} from "@gorhom/bottom-sheet";
import type { BottomSheetBackdropProps } from "@gorhom/bottom-sheet";
import { type PropsWithChildren, createContext, useCallback, useContext, useMemo, useRef } from "react";
import { View } from "react-native";
import { StyleSheet } from "react-native-unistyles";

import { Button, Surface, Typography } from "@/components/ui";
import { useSession } from "@/features/auth";

type LoginSheetContextValue = {
presentLoginSheet: () => void;
dismissLoginSheet: () => void;
};

const LoginSheetContext = createContext
<LoginSheetContextValue
| null>(null);

function renderBackdrop(props: BottomSheetBackdropProps) {
  return (
<BottomSheetBackdrop
        appearsOnIndex={0}
disappearsOnIndex={-1}
pressBehavior="close"
{...props}
/>
);
}

function LoginSheetBody({ onAuthed }: { onAuthed: () => void }) {
const { signIn } = useSession();

  return (
<BottomSheetScrollView contentContainerStyle={styles.sheetContent}>
<Surface style={styles.card}>
<Typography variant="label">Giris</Typography>
<Typography variant="title">Hesabiniza girin</Typography>
<Typography color="muted">
    Tab ve korumali icerige erismek icin oturum acin. Auth tamamlandiginda bu sheet
    gercek form ile degistirilebilir.
</Typography>
<Button
        label="Demo ile giris yap"
        onPress={async () => {
await signIn();
onAuthed();
}}
style={styles.fullWidth}
variant="primary"
/>
</Surface>
        </BottomSheetScrollView>
        );
}

        export function LoginSheetProvider({ children }: PropsWithChildren) {
        const sheetRef = useRef<BottomSheetModal>(null);

const presentLoginSheet = useCallback(() => {
sheetRef.current?.present();
}, []);

const dismissLoginSheet = useCallback(() => {
sheetRef.current?.dismiss();
}, []);

const contextValue = useMemo<LoginSheetContextValue>(
() => ({
dismissLoginSheet,
presentLoginSheet,
}),
[dismissLoginSheet, presentLoginSheet],
);

  return (
<BottomSheetModalProvider>
    <LoginSheetContext.Provider value={contextValue}>
    {children}
    <BottomSheetModal
            ref={sheetRef}
    backdropComponent={renderBackdrop}
    enableDynamicSizing={false}
    enablePanDownToClose
    index={0}
    snapPoints={["45%", "88%"]}
    >
    <View style={styles.sheetRoot}>
    <LoginSheetBody onAuthed={dismissLoginSheet} />
</View>
</BottomSheetModal>
        </LoginSheetContext.Provider>
        </BottomSheetModalProvider>
        );
}

        export function useLoginSheet() {
        const ctx = useContext(LoginSheetContext);
        if (!ctx) {
        throw new Error("useLoginSheet must be used within LoginSheetProvider");
        }
        return ctx;
        }

        const styles = StyleSheet.create((theme) => ({
        sheetContent: {
        paddingBottom: theme.spacing.xl,
        paddingHorizontal: theme.spacing.md,
        paddingTop: theme.spacing.sm,
        },
        sheetRoot: {
        flex: 1,
        },
        card: {
        borderRadius: theme.radius.xl,
        gap: theme.spacing.md,
        },
        fullWidth: {
        alignSelf: "stretch",
        },
        }));
</file>

<file path="src/services/auth/hooks.ts">
import { useMutation, useQuery, useQueryClient } from '@tanstack/react-query';

import { sessionStore, useSessionStore } from '@/features/auth/store/session-store';
import { queryKeys } from '@/services/api/query-keys';

import { authService } from './service';
import type { LoginDto } from './types';

export function useLogin() {
const queryClient = useQueryClient();

return useMutation({
mutationFn: (data: LoginDto) => authService.login(data),
onSuccess: async (data) => {
await sessionStore.getState().setSessionTokens(data.accessToken, data.refreshToken);
try {
const me = await authService.getMe();
queryClient.setQueryData(queryKeys.auth.me(), me);
} catch {
await queryClient.invalidateQueries({ queryKey: queryKeys.auth.me() });
}
},
});
}

export function useRegister() {
return useMutation({
mutationFn: authService.register,
});
}

export function useLogout() {
const queryClient = useQueryClient();

return useMutation({
mutationFn: authService.logout,
onSettled: async () => {
await sessionStore.getState().signOut();
queryClient.clear();
},
});
}

export function useMe(options?: { enabled?: boolean }) {
const isAuthed = useSessionStore(
(s) => s.status === 'authenticated' && !!s.sessionToken,
);
const enabled = (options?.enabled ?? true) && isAuthed;

return useQuery({
queryKey: queryKeys.auth.me(),
queryFn: () => authService.getMe(),
enabled,
staleTime: 5 * 60 * 1000,
});
}

export function useForgotPassword() {
return useMutation({
mutationFn: authService.forgotPassword,
});
}

export function useResetPassword() {
return useMutation({
mutationFn: authService.resetPassword,
});
}

export function useVerifyEmail(token: string | null) {
return useQuery({
queryKey: queryKeys.auth.verifyEmail(token),
queryFn: () => authService.verifyEmail(token!),
enabled: !!token,
retry: false,
});
}
</file>

<file path="src/services/auth/index.ts">
export type * from './types';
export { authService } from './service';
export {
useForgotPassword,
useLogin,
useLogout,
useMe,
useRegister,
useResetPassword,
useVerifyEmail,
} from './hooks';
</file>

<file path="src/services/auth/service.ts">
import { apiClient } from '@/services/api/api-client';

import type {
RegisterDto,
LoginDto,
AuthResponse,
ForgotPasswordDto,
ResetPasswordDto,
User,
} from './types';

interface MeResponse {
id: string;
email: string;
type?: 'user' | 'business';
firstName?: string;
lastName?: string;
phoneNumber?: string;
address?: string;
jobPostCredits?: number;
emailVerified?: boolean;
isEmailVerified?: boolean;
createdAt: string;
updatedAt: string;
profile?: {
firstName?: string;
lastName?: string;
address?: string;
} | null;
}

function normalizeUser(data: MeResponse): User {
const credits = data.jobPostCredits ?? 0;

return {
id: data.id,
email: data.email,
type: data.type ?? (credits > 0 ? 'business' : 'user'),
firstName: data.firstName ?? data.profile?.firstName ?? '',
lastName: data.lastName ?? data.profile?.lastName ?? '',
phoneNumber: data.phoneNumber ?? '',
address: data.address ?? data.profile?.address ?? '',
jobPostCredits: credits,
emailVerified: data.emailVerified ?? data.isEmailVerified ?? false,
createdAt: data.createdAt,
updatedAt: data.updatedAt,
};
}

export const authService = {
register: async (data: RegisterDto) => {
const response = await apiClient.post('/auth/register', data);
return response.data;
},

login: async (data: LoginDto) => {
const response = await apiClient.post<AuthResponse>('/auth/login', data);
return response.data;
},

verifyEmail: async (token: string) => {
const response = await apiClient.get('/auth/verify-email', {
params: { token },
});
return response.data;
},

refreshToken: async () => {
const response = await apiClient.post<AuthResponse>('/auth/refresh');
return response.data;
},

logout: async () => {
const response = await apiClient.post('/auth/logout');
return response.data;
},

getMe: async () => {
const response = await apiClient.get<MeResponse>('/auth/me');
return normalizeUser(response.data);
},

forgotPassword: async (data: ForgotPasswordDto) => {
const response = await apiClient.post('/auth/forgot-password', data);
return response.data;
},

resetPassword: async (data: ResetPasswordDto) => {
const response = await apiClient.post('/auth/reset-password', data);
return response.data;
},
};
</file>

<file path="src/services/auth/types.ts">
export interface RegisterDto {
email: string;
password: string;
phoneNumber: string;
firstName: string;
lastName: string;
address: string;
birthDate?: string;
gender?: 'MALE' | 'FEMALE' | 'OTHER';
}

export interface LoginDto {
email: string;
password: string;
}

export interface AuthResponse {
accessToken: string;
refreshToken: string;
}

export interface ForgotPasswordDto {
email: string;
}

export interface ResetPasswordDto {
token: string;
newPassword: string;
}

export interface User {
id: string;
email: string;
type: 'user' | 'business';
firstName: string;
lastName: string;
phoneNumber: string;
address: string;
jobPostCredits: number;
emailVerified: boolean;
createdAt: string;
updatedAt: string;
}
</file>

<file path="src/services/jobs/hooks.ts">
import {
useInfiniteQuery,
useMutation,
useQuery,
useQueryClient,
} from '@tanstack/react-query';

import { queryKeys } from '@/services/api/query-keys';

import { jobsService } from './service';
import type {
ApplicationStatus,
CreateJobDto,
GetJobsParams,
PaginationParams,
UpdateJobDto,
UpdateMyApplicationNoteDto,
} from './types';

export function useJobs(params?: GetJobsParams) {
return useQuery({
queryKey: queryKeys.jobs.list(params),
queryFn: () => jobsService.getAll(params),
});
}

type InfiniteJobsParams = Omit<GetJobsParams, 'page' | 'limit'> & {
initialLimit?: number;
};

export function useInfiniteJobs(params?: InfiniteJobsParams) {
const { initialLimit: initialLimitParam, ...apiParams } = params ?? {};
const initialLimit = initialLimitParam ?? 20;

return useInfiniteQuery({
queryKey: queryKeys.jobs.infinite(params),
initialPageParam: { page: 1 },
queryFn: ({ pageParam }) =>
jobsService.getAll({
...apiParams,
page: pageParam.page,
limit: initialLimit,
}),
getNextPageParam: (lastPage) => {
if (!lastPage.hasNextPage) {
return undefined;
}

return {
page: lastPage.page + 1,
};
},
});
}

export function useJob(id: string) {
return useQuery({
queryKey: queryKeys.jobs.detail(id),
queryFn: () => jobsService.getById(id),
enabled: !!id,
});
}

export function useMyJobs(params?: PaginationParams) {
return useQuery({
queryKey: queryKeys.jobs.my(params),
queryFn: () => jobsService.getMyJobs(params),
});
}

export function useCreateJob() {
const queryClient = useQueryClient();
return useMutation({
mutationFn: (data: CreateJobDto) => jobsService.create(data),
onSuccess: () => {
queryClient.invalidateQueries({ queryKey: queryKeys.jobs.all });
queryClient.invalidateQueries({ queryKey: queryKeys.auth.me() });
},
});
}

export function useUpdateJob() {
const queryClient = useQueryClient();
return useMutation({
mutationFn: ({ id, data }: { id: string; data: UpdateJobDto }) =>
jobsService.update(id, data),
onSuccess: (_, variables) => {
queryClient.invalidateQueries({ queryKey: queryKeys.jobs.all });
queryClient.invalidateQueries({ queryKey: queryKeys.jobs.detail(variables.id) });
queryClient.invalidateQueries({ queryKey: queryKeys.jobs.my() });
},
});
}

export function useDeleteJob() {
const queryClient = useQueryClient();
return useMutation({
mutationFn: (id: string) => jobsService.delete(id),
onSuccess: () => {
queryClient.invalidateQueries({ queryKey: queryKeys.jobs.all });
},
});
}

export function useApplyToJob() {
const queryClient = useQueryClient();
return useMutation({
mutationFn: (jobId: string) => jobsService.applyToJob(jobId),
onSuccess: () => {
queryClient.invalidateQueries({ queryKey: queryKeys.jobs.all });
},
});
}

export function useJobApplications(jobId: string) {
return useQuery({
queryKey: queryKeys.jobs.applications(jobId),
queryFn: () => jobsService.getJobApplications(jobId),
enabled: !!jobId,
});
}

export function useUpdateApplicationStatus() {
const queryClient = useQueryClient();
return useMutation({
mutationFn: ({
jobId,
id,
status,
}: {
jobId: string;
id: string;
status: ApplicationStatus;
}) => jobsService.updateApplicationStatus(id, { status }),
onSuccess: (_, { jobId }) => {
queryClient.invalidateQueries({ queryKey: queryKeys.jobs.applications(jobId) });
queryClient.invalidateQueries({ queryKey: queryKeys.jobs.all });
},
});
}

export function useMyApplications(params?: PaginationParams) {
return useQuery({
queryKey: queryKeys.jobs.myApplications(params),
queryFn: () => jobsService.getMyApplications(params),
});
}

export function useUpdateMyApplicationNote() {
const queryClient = useQueryClient();
return useMutation({
mutationFn: ({
id,
data,
}: {
id: string;
data: UpdateMyApplicationNoteDto;
}) => jobsService.updateMyApplicationNote(id, data),
onSuccess: () => {
queryClient.invalidateQueries({ queryKey: [...queryKeys.jobs.all, 'my-applications'] });
},
});
}
</file>

<file path="src/services/jobs/index.ts">
export type * from './types';
export { jobsService } from './service';
export {
useApplyToJob,
useCreateJob,
useDeleteJob,
useInfiniteJobs,
useJob,
useJobApplications,
useJobs,
useMyApplications,
useMyJobs,
useUpdateApplicationStatus,
useUpdateJob,
useUpdateMyApplicationNote,
} from './hooks';
</file>

<file path="src/services/jobs/service.ts">
import { apiClient } from '@/services/api/api-client';

import type {
GetJobsParams,
GetJobsResponse,
Job,
CreateJobDto,
UpdateJobDto,
JobApplication,
UpdateApplicationStatusDto,
UpdateMyApplicationNoteDto,
PaginationParams,
MyJobsResponse,
MyApplicationsResponse,
} from './types';

export const jobsService = {
getAll: async (params?: GetJobsParams): Promise
<GetJobsResponse>
=> {
const response = await apiClient.get<GetJobsResponse>('/jobs', { params });
return response.data;
},

getById: async (id: string): Promise
<Job>
=> {
const response = await apiClient.get<Job>(`/jobs/${id}`);
return response.data;
},

create: async (data: CreateJobDto): Promise
<Job>
=> {
const response = await apiClient.post<Job>('/jobs', data);
return response.data;
},

update: async (id: string, data: UpdateJobDto): Promise
<Job>
=> {
const response = await apiClient.patch<Job>(`/jobs/${id}`, data);
return response.data;
},

delete: async (id: string): Promise
<void>
=> {
await apiClient.delete(`/jobs/${id}`);
},

getMyJobs: async (params?: PaginationParams): Promise
<MyJobsResponse>
=> {
const response = await apiClient.get<MyJobsResponse>('/jobs/my-jobs', { params });
return response.data;
},

applyToJob: async (jobId: string): Promise
<void>
=> {
await apiClient.post(`/jobs/${jobId}/apply`);
},

getJobApplications: async (jobId: string): Promise<JobApplication[]> => {
const response = await apiClient.get<JobApplication[]>(
`/jobs/${jobId}/applications`,
);
return response.data;
},

updateApplicationStatus: async (
id: string,
data: UpdateApplicationStatusDto,
): Promise
<JobApplication>
=> {
const response = await apiClient.patch<JobApplication>(
`/jobs/applications/${id}/status`,
data,
);
return response.data;
},

getMyApplications: async (
params?: PaginationParams,
): Promise
<MyApplicationsResponse>
=> {
const response = await apiClient.get<MyApplicationsResponse>(
'/jobs/applications/my-applications',
{ params },
);
return response.data;
},

updateMyApplicationNote: async (
id: string,
data: UpdateMyApplicationNoteDto,
): Promise
<JobApplication>
=> {
const response = await apiClient.patch<JobApplication>(
`/jobs/applications/${id}/my-note`,
data,
);
return response.data;
},
};
</file>

<file path="src/services/jobs/types.ts">
export type PromotionLevel = 'NONE' | 'BASIC' | 'PREMIUM';
export type JobStatus = 'OPEN' | 'IN_PROGRESS' | 'COMPLETED' | 'CANCELLED';
export type ApplicationStatus = 'PENDING' | 'ACCEPTED' | 'REJECTED';
export type FollowUpStatus = 'WENT' | 'DID_NOT_GO' | 'NO_RESPONSE';
export type WeekDay = 'MON' | 'TUE' | 'WED' | 'THU' | 'FRI' | 'SAT' | 'SUN';

export interface Job {
id: string;
title: string;
description: string;
city: string;
district: string;
detailedAddress?: string;
jobCategory: string;
promotionLevel: PromotionLevel;
dailyWage: number;
neededWorkers?: number;
hasOvertime?: boolean;
overtimeWage?: number;
providedBenefits?: string[];
shiftStartTime: string;
shiftEndTime: string;
startDate: string;
endDate?: string;
latitude?: number;
longitude?: number;
location?: {
lat: number;
lng: number;
} | null;
status: JobStatus;
createdAt: string;
updatedAt: string;
userId: string;
user?: {
firstName: string;
lastName: string;
};
applicationsCount?: number;
}

export interface CreateJobDto {
title: string;
description?: string;
city: string;
district: string;
detailedAddress?: string;
jobCategory: string;
promotionLevel?: PromotionLevel;
dailyWage: number;
neededWorkers?: number;
hasOvertime?: boolean;
overtimeWage?: number;
providedBenefits?: string[];
shiftStartTime: string;
shiftEndTime: string;
startDate: string;
endDate?: string;
latitude?: number;
longitude?: number;
}

export interface UpdateJobDto {
title?: string;
description?: string;
city?: string;
district?: string;
detailedAddress?: string;
jobCategory?: string;
promotionLevel?: PromotionLevel;
dailyWage?: number;
neededWorkers?: number;
hasOvertime?: boolean;
overtimeWage?: number;
providedBenefits?: string[];
shiftStartTime?: string;
shiftEndTime?: string;
startDate?: string;
endDate?: string;
latitude?: number;
longitude?: number;
status?: JobStatus;
}

export interface UpdateApplicationStatusDto {
status: ApplicationStatus;
}

export interface JobApplication {
id: string;
jobId: string;
userId: string;
status: ApplicationStatus;
applicantFollowUpStatus?: FollowUpStatus | null;
applicantNote?: string | null;
applicantNotedAt?: string | null;
appliedAt: string;
job?: {
id: string;
title: string;
city?: string;
district?: string;
shiftStartTime?: string;
shiftEndTime?: string;
startDate?: string;
dailyWage?: number;
};
user?: {
firstName?: string;
lastName?: string;
email?: string;
phoneNumber?: string;
profile?: {
firstName?: string;
lastName?: string;
birthDate?: string;
gender?: 'MALE' | 'FEMALE' | 'OTHER';
availableDays?: WeekDay[];
capabilities?: string[];
};
};
}

export interface UpdateMyApplicationNoteDto {
followUpStatus?: FollowUpStatus;
note?: string;
}

export interface PaginationParams {
page?: number;
limit?: number;
}

export interface PaginatedResponse
<T>
{
data: T[];
total: number;
page: number;
limit: number;
totalPages: number;
hasNextPage: boolean;
}

export interface GetJobsParams {
page?: number;
limit?: number;
category?: string;
jobCategory?: string;
latitude?: number;
longitude?: number;
radiusInMeters?: number;
}

export type GetJobsResponse = PaginatedResponse<Job>;
export type MyJobsResponse = PaginatedResponse<Job>;
export type MyApplicationsResponse = PaginatedResponse<JobApplication>;
</file>

<file path="src/services/payment/hooks.ts">
import { useQuery } from '@tanstack/react-query';

import { queryKeys } from '@/services/api/query-keys';

import { paymentService } from './service';

export function useCreditPlans() {
return useQuery({
queryKey: queryKeys.payment.creditPlans(),
queryFn: paymentService.getCreditPlans,
});
}
</file>

<file path="src/services/payment/index.ts">
export type * from './types';
export { paymentService } from './service';
export { useCreditPlans } from './hooks';
</file>

<file path="src/services/payment/service.ts">
import { apiClient } from '@/services/api/api-client';

import type { CreditPlan } from './types';

export const paymentService = {
getCreditPlans: async () => {
const response = await apiClient.get<CreditPlan[]>('/payment/credit-plans');
return response.data;
},
};
</file>

<file path="src/services/payment/types.ts">
export interface SubscribeDto {
amount: number;
}

export interface CreditPlan {
key: string;
name: string;
credits: number;
amount: number;
currency: string;
shopierUrl: string;
}
</file>

<file path="src/services/profile/hooks.ts">
import { useMutation, useQuery, useQueryClient } from '@tanstack/react-query';

import { queryKeys } from '@/services/api/query-keys';

import { profileService } from './service';
import type { CreateProfileDto, NearbyParams, UpdateProfileDto } from './types';

export function useProfile(enabled = true) {
return useQuery({
queryKey: queryKeys.profile.detail(),
queryFn: profileService.getProfile,
enabled,
});
}

export function useCreateProfile() {
const queryClient = useQueryClient();
return useMutation({
mutationFn: (data: CreateProfileDto) => profileService.createProfile(data),
onSuccess: () => {
queryClient.invalidateQueries({ queryKey: queryKeys.profile.all });
    },
});
}

export function useUpdateProfile() {
const queryClient = useQueryClient();
return useMutation({
mutationFn: (data: UpdateProfileDto) => profileService.updateProfile(data),
onSuccess: () => {
queryClient.invalidateQueries({ queryKey: queryKeys.profile.all });
    },
});
}

export function useNearbyProfiles(params: NearbyParams) {
return useQuery({
queryKey: queryKeys.profile.nearby(params),
queryFn: () => profileService.findNearby(params),
enabled: !!params.lat && !!params.lng,
});
}
</file>

<file path="src/services/profile/index.ts">
export type * from './types';
export { profileService } from './service';
export { useCreateProfile, useNearbyProfiles, useProfile, useUpdateProfile } from './hooks';
</file>

<file path="src/services/profile/service.ts">
import { apiClient } from '@/services/api/api-client';

import type { Profile, CreateProfileDto, UpdateProfileDto, NearbyParams } from './types';

export const profileService = {
getProfile: async () => {
const response = await apiClient.get<Profile>('/profile');
return response.data;
},

createProfile: async (data: CreateProfileDto) => {
const response = await apiClient.post<Profile>('/profile', data);
return response.data;
},

updateProfile: async (data: UpdateProfileDto) => {
const response = await apiClient.put<Profile>('/profile', data);
return response.data;
},

findNearby: async (params: NearbyParams) => {
const response = await apiClient.get<Profile[]>('/profile/nearby', { params });
return response.data;
},
};
</file>

<file path="src/services/profile/types.ts">
export type Gender = 'MALE' | 'FEMALE' | 'OTHER';
export type WeekDay = 'MON' | 'TUE' | 'WED' | 'THU' | 'FRI' | 'SAT' | 'SUN';

export interface Profile {
id: string;
userId: string;
firstName: string;
lastName: string;
address: string;
birthDate?: string | null;
gender?: Gender | null;
capabilities: string[];
availability: string[];
availableDays: WeekDay[];
isAvailable: boolean;
longitude: number | null;
latitude: number | null;
createdAt: string;
updatedAt: string;
}

export interface CreateProfileDto {
firstName: string;
lastName: string;
address: string;
birthDate?: string;
gender?: Gender;
capabilities?: string[];
availability?: string[];
availableDays?: WeekDay[];
isAvailable?: boolean;
longitude?: number;
latitude?: number;
}

export interface UpdateProfileDto {
firstName?: string;
lastName?: string;
address?: string;
birthDate?: string;
gender?: Gender;
capabilities?: string[];
availability?: string[];
availableDays?: WeekDay[];
isAvailable?: boolean;
longitude?: number;
latitude?: number;
}

export interface NearbyParams {
lng: string;
lat: string;
radius?: string;
day?: WeekDay;
}
</file>

<file path=".prettierrc.json">
{
"printWidth": 100,
"tabWidth": 2,
"singleQuote": true,
"bracketSameLine": true
}
</file>

<file path=".agents/rules/rule.mdc">
---
alwaysApply: true
---

-- local storage için mmkv kullan, zustand persist ederken her zaman mmkv kullan.
-- styles için her zaman react-native-unistyles kullan.
-- eğer bir iş gelirse ilk olarak skills kısmında ihtiyacın olabilecek durumları incele ve o şekilde ilerle.
-- AGENTS.md dosyasını güncellemek gerekecektir bunun için benden onay al, değişen bir yapı olur yada kullanımı, konumu
değişirse bir sistemdeki parçanın bana sor güncellemek için ve neyi güncelelyeceğinide göster ve güncelle.
--
</file>

<file path=".agents/AGENTS.md">
# ae-mobil Agent Guide

Bu dosya, bu repo icinde calisan agent'lar icin proje mimarisi ve kullanim kurallarini anlatir.

Amaç:

- Yeni gelen agent'larin mevcut foundation'i hizli anlamasi
- Tekrarlanan mimari kararlarin bozulmamasi
- Yeni kodun mevcut pattern'lerle uyumlu yazilmasi

## 1. Genel Mimari

Bu proje Expo + Expo Router + **react-native-unistyles 3.x** + Zustand + React Query + Axios + i18next tabanli bir mobil
app foundation'idir.

Ana klasor mantigi:

- `app/`: Expo Router route dosyalari ve layout'lar
- `src/components/`: shared UI primitive ve component'ler
- `src/core/`: provider, navigation, app-level altyapi
- `src/features/`: feature bazli ekranlar, auth, app flow
- `src/i18n/`: locale, translation, direction kurallari
- `src/services/`: API client, query client, remote data yardimcilari
- `src/store/`: global app store
- `src/theme/`: token'lar ve Unistyles yapilandirmasi
- `docs/`: insana donuk mimari notlari

Kritik ilke:

- Route mantigi `app/` altinda
- Asil ekran/component implementasyonu `src/features/` ve `src/components/` altinda
- `app/` dosyalari ince route adapter olarak kalmali

## 2. Theme Sistemi (Unistyles)

Theme altyapisi:

- `src/theme/tokens.ts` — `lightTheme` / `darkTheme`, `spacing`, `radius`, semantic renk contract
- `src/theme/unistyles.ts` — `StyleSheet.configure({ themes, settings })`; **uygulama girisinde** `@/theme/unistyles`
import edilir (`app/_layout.tsx`)
- `babel.config.js` — `react-native-unistyles/plugin` (`root: "src"`)
- `src/core/providers/theme-provider.tsx` — React context ile `AppTheme` (tokens); tercih / mantik icin
- `src/core/providers/theme-scope.tsx` — `UnistylesRuntime.setTheme(theme.id)` + root arka plan

Calisma mantigi:

1. Design token'lar ve semantic tema `tokens.ts` icindedir.
2. Unistyles temalari `unistyles.ts` ile `light` / `dark` olarak kayitlidir.
3. `ThemeScope`, Zustand'daki tema tercihine gore runtime tema degistirir (`theme.id` = `"light"` | `"dark"`).

Kurallar:

- Renk/spacing icin **birebir hex hard-code** etmek yerine `StyleSheet.create((theme) => (...))` icinde
`theme.colors.*`, `theme.spacing.*`, `theme.radius.*` kullan.
- **NativeWind, `className`, `tailwind.config.js` kullanma** — bu proje Unistyles 3 ile kurgulanmistir.
- Yeni semantic renk veya token eklenirse once `tokens.ts` (ve bagimli tema tipleri) guncellenmeli, sonra ilgili
component stilleri.

## 3. Styling (react-native-unistyles)

Styling altyapisi:

- `react-native-unistyles`
- `StyleSheet` importu: `import { StyleSheet } from "react-native-unistyles"`
- `babel.config.js` icindeki Unistyles plugin

Shared component styling mantigi:

- Varsayilan stil `StyleSheet.create((theme) => ({ ... }))` ile theme bagimli objelerdir.
- Shared component'lerde `variant`, `tone`, `color` gibi **semantic prop** API'leri tercih edilir.
- `style` prop'u yalnizca disaridan override veya edge-case icin kullanilir; ana gorunum yine `styles.*` uzerinden
gelir.

Kurallar:

- Yeni UI component yazarken ilk tercih **Unistyles `StyleSheet.create`**.
- `theme` disinda kalan tek seferlik degerler icin bile mumkunse token'lara yakin tut (constants veya theme extension).
- RTL ve layout icin `AppText` ve mevcut primitive'lerdeki ornekleri takip et.

Ornekler:

- `src/components/ui/app-text.tsx`
- `src/components/ui/app-button.tsx`
- `src/components/ui/surface-card.tsx`
- `src/components/ui/app-screen.tsx`

Insan dokumanı: `docs/unistyles.md`

## 4. Shared Component Sistemi

Mevcut primitive'ler:

- `AppScreen`
- `AppText`
- `AppButton`
- `SurfaceCard`

Dosya:

- `src/components/ui/index.ts`

Kurallar:

- Ekranlar dogrudan ham `Text`/`View` yerine mümkün oldugunca shared primitive kullanmali.
- Typography ihtiyaclari icin once `AppText` genisletilmeli.
- Button varyasyonlari icin once `AppButton` genisletilmeli.
- Layout ve page padding ihtiyaci icin once `AppScreen` kullanilmali.

RTL notu:

- `AppText` locale direction farkindaligina sahiptir.
- Yeni shared component yazarken RTL dusunulmeli.
- `start/end`, metin hizasi, `flex-row` gibi kararlar locale yonu ile uyumlu tasarlanmalı.

## 5. i18n ve Dil Sistemi

Ana dosyalar:

- `src/i18n/index.ts`
- `src/i18n/locale.ts`
- `src/i18n/translations/index.ts`
- `src/i18n/translations/en.ts`
- `src/i18n/translations/tr.ts`
- `src/i18n/translations/ar.ts`

Desteklenen diller:

- `en`
- `tr`
- `ar`

Direction mantigi:

- Locale metadata `src/i18n/locale.ts` icinde tutulur.
- `ar` RTL kabul edilir.
- `useLocaleSettings()` locale, `direction`, `isRTL` dondurur.

Kurallar:

- Yeni dil eklenecekse:
  - translation dosyasi ekle
  - `translations/index.ts` export et
  - `locale.ts` icinde supported locale listesine ekle
  - `app.json` icinde supported locales listesine ekle
- i18n key'leri feature/alan mantigiyla gruplanmali.
- Hard-coded text yerine `t(...)` tercih edilmeli.

RTL notu:

- Expo docs'a gore `extra.supportsRTL: true` ve `expo-localization` plugin ayarlari native seviyede etkilidir.
- Uygulama ici direction davranisi component seviyesinde de desteklenir.

## 6. Router ve Navigation Sistemi

Ana dosyalar:

- `app/_layout.tsx`
- `app/(onboarding)/...`
- `app/(app)/...`
- `app/(auth)/...`
- `app/(app)/(tabs)/_layout.tsx`
- `src/core/navigation/paths.ts`
- `docs/expo-router.md`

Route group mantigi (guncel kok stack):

- `(onboarding)`: onboarding tamamlanincaya kadar (`!hasCompletedOnboarding`)
- `(app)` ve `(auth)`: onboarding tamamlandiktan sonra (`hasCompletedOnboarding`); ana uygulama sekmeleri + ayri auth
flow ekranlari (auth, app ile **ayni kok korumasi altinda kardes** grup — auth ekranlari `(app)/_layout` stack'ine
gomulmez)
- `(app)/(tabs)`: ana tab navigator
- `app/modal.tsx`: global modal route

Auth korumasi:

- Kok `Stack.Protected` onboarding ve post-onboarding gruplarini ayirir.
- Oturum / tab bazli kisitlar icin ayrica `Tabs.Protected`, layout guard'lari veya login sheet gibi pattern'ler
kullanilabilir.

Tab bar kurallari:

- Kalici sekmeler `(tabs)` altindadir
- Route var ama sekme butonu gosterilmeyecekse `href: null`
- Tab bar gizlenecek tam ekran akislari tabs grubunun disinda, ayni `(app)` stack altinda sibling route olarak acilir

Ornek:

- `app/(app)/(tabs)/home.tsx`
- `app/(app)/details/[id].tsx`

Modal kurali:

- Navigation history'nin parcasi olacaksa Expo Router modal route
- Basit local popup ise React Native `Modal`

## 7. Animated Tab Bar

Ana dosyalar:

- `src/core/navigation/animated-tab-bar.tsx`
- `src/core/navigation/tab-bar-visibility-context.tsx`
- `src/components/ui/app-screen.tsx`

Calisma mantigi:

- Tabs custom animated tab bar render eder
- `AppScreen` scroll davranisi ile tab bar'i hide/show eder
- Tabs disindaki ekranlarda bu davranis aktif degildir

Lock mantigi:

- `AppScreen` icinde `tabBarScrollBehavior="locked"` kullanilabilir
- Bu durumda scroll ile tab bar gizlenmez

Kural:

- Scroll davranisi gereken tab ekranlarinda `AppScreen` kullan
- Tab bar'in sabit kalmasi gereken ekranlarda `tabBarScrollBehavior="locked"` kullan

## 8. Store Kurallari

Ana dosyalar:

- `src/store/app-store.ts`
- `src/features/auth/store/session-store.ts`

Store bolunumu:

- `app-store`: app-wide UI ve locale/theme/navigation state
- `session-store`: auth/session state

Kurallar:

- Zustand selector'lari mumkunse primitive dondurmeli
- Obje/array selector'lari gereksiz render ve loop riski olusturabilir
- Guvenli pattern:
  - `const theme = useAppStore((state) => state.themePreference)`
  - `const toggle = useAppStore((state) => state.toggleThemePreference)`
- Asagidaki gibi selector'lardan kacinin:
  - `useAppStore((state) => ({ theme: state.themePreference }))`

Server state kurali:

- Server state Zustand'da tutulmaz
- Server state React Query tarafinda tutulur

## 9. Services ve API Katmani

Ana dosyalar:

- `src/services/api/api-client.ts`
- `src/services/api/query-client.ts`
- `src/services/api/query-keys.ts`
- `src/services/api/normalize-api-error.ts`

Kurallar:

- Ekran icinden dogrudan `fetch` kullanma
- API cagrilari service/query katmanindan gecmeli
- Query key'ler merkezi tanimlanmali
- Axios error'lari normalize edilerek handle edilmeli
- **Kimlik dogrulama:** `api-client` istek interceptor'inda `expo-secure-store` uzerinden okunan access token varsa
`Authorization: Bearer<token>` eklenir (`src/services/storage/secure-storage.ts`). Token yoksa baslik eklenmez (public
endpoint'ler).

Gelecek pattern:

- Feature bazli query hook'lari `src/features/<feature>/api` ya da `hooks` altinda tutulabilir
- Ortak axios instance kullanilmali
- Ileride refresh token / 401 retry icin response interceptor genisletilebilir

## 10. Auth ve Session

Ana dosyalar:

- `src/features/auth/hooks/use-session.ts`
- `src/features/auth/store/session-store.ts`
- `src/features/auth/screens/sign-in-screen.tsx`
- `src/services/storage/secure-storage.ts`

Su anki durum:

- Session token `SecureStore`'da; `session-store` bootstrap ile senkronize eder
- `bootstrap()` ilk acilista token okur

Gelecek plan:

- Token refresh / session restore
- Role bazli nested protected routes (gerekiyorsa)

## 11. Route Helper Kullanimi

Dosya:

- `src/core/navigation/paths.ts`

Kurallar:

- Hard-coded route string yerine `paths` kullan
- Dynamic route'lar typed route uyumlu helper ile donmeli
- Router gecislerinde ilk tercih:
  - `router.push(paths.settings)`
  - `router.push(paths.details("42"))`

## 12. Kod Yazim Kurallari

Bu projede yeni agent'lar sunlari korumali:

- `app/` altinda logic buyutme, logic'i `src/` altinda tut
- Yeni sayfa = route adapter + feature screen ayrimi
- Shared component varsa onu genislet, benzer component'i kopyalama
- **Stil: Unistyles + theme token'lari** — className / Tailwind yok
- Hard-coded text yazma, i18n kullan
- Zustand selector'da object return etme
- Tabs icinde full-screen secondary page gerekiyorsa tabs disi sibling stack kullan
- Scroll-hide gereken yerlerde `AppScreen` kullan

## 13. Yararlı Referanslar

Proje ici:

- `docs/expo-router.md`
- `docs/unistyles.md`
- `.agents/rules/rule.mdc`

Harici resmi referanslar:

- React Native Unistyles 3
- Expo Router Authentication
- Expo Router Protected Routes
- Expo Router Tabs
- Expo Router Modals
- Expo Localization / RTL support

## 14. Agent Checklist

Bu repo'da degisiklik yapmadan once:

1. Degisiklik hangi katmanda?
   - route / feature / shared component / store / service / theme
2. Mevcut pattern var mi?
   - varsa onu genislet
3. Text i18n'e girmeli mi?
4. Renk ve spacing `tokens.ts` + Unistyles `theme` uzerinden mi tanimlanmali?
5. Zustand selector primitive mi?
6. Route degisikligi `paths.ts` ile uyumlu mu?
7. RTL etkisi var mi?
8. API cagrisi `api-client` ile mi (token interceptor davranisini bozmadan)?

Bu checklist yeni agent'in mevcut foundation'i bozmadan ilerlemesini saglar.
</file>

<file path="app/(app)/(tabs)/_layout.tsx">
import { Ionicons } from '@expo/vector-icons';
import { Tabs } from 'expo-router';

import { AnimatedTabBar } from '@/core/navigation/animated-tab-bar';
import { TabBarVisibilityProvider } from '@/core/navigation/tab-bar-visibility-context';

export default function AppTabsLayout() {
  return (
    <TabBarVisibilityProvider>
      <Tabs
        screenOptions={{
          headerShown: false,
          tabBarHideOnKeyboard: true,
          tabBarStyle: {
            borderTopWidth: 1,
            height: 72,
            paddingBottom: 10,
            paddingTop: 8,
        position: 'absolute',
          },
        }}
        tabBar={(props) =>
        <AnimatedTabBar
        {...props} />}>
        <Tabs.Screen
          name="home"
          options={{
        title: 'Home',
            tabBarIcon: ({ color, size }) => (
              <Ionicons color={color} name="home-outline" size={size} />
            ),
          }}
        />
        <Tabs.Screen
          name="search"
          options={{
        title: 'Search',
            tabBarIcon: ({ color, size }) => (
              <Ionicons color={color} name="search-outline" size={size} />
            ),
          }}
        />
        <Tabs.Screen
          name="profile"
          options={{
        title: 'Profile',
            tabBarIcon: ({ color, size }) => (
              <Ionicons color={color} name="person-outline" size={size} />
            ),
          }}
        />
        <Tabs.Screen
          name="internal"
          options={{
            href: null,
          }}
        />
      </Tabs>
    </TabBarVisibilityProvider>
  );
}
</file>

<file path="app/(app)/(tabs)/home.tsx">
import { AppHomeScreen } from '@/features/app/screens/app-home-screen';

export default function HomeTabRoute() {
  return <AppHomeScreen />;
}
</file>

<file path="app/(app)/(tabs)/internal.tsx">
import { InternalRouteScreen } from "@/features/app/screens/internal-route-screen";

export default function InternalTabRoute() {
  return <InternalRouteScreen />;
}
</file>

<file path="app/(app)/(tabs)/search.tsx">
import { SearchScreen } from "@/features/app/screens/search-screen";

export default function SearchTabRoute() {
  return <SearchScreen />;
}
</file>

<file path="app/(app)/details/[id].tsx">
import { DetailsScreen } from "@/features/app/screens/details-screen";

export default function DetailsRoute() {
  return <DetailsScreen />;
}
</file>

<file path="app/(app)/_layout.tsx">
import { Stack } from 'expo-router';

export const unstable_settings = {
initialRouteName: '(tabs)',
};

export default function AppLayout() {
  return (
    <Stack screenOptions={{ headerShown: false }}>
      <Stack.Screen name="(tabs)" />
      <Stack.Screen
        name="details/[id]"
        options={{
          headerShown: true,
title: 'Details',
        }}
      />
      <Stack.Screen
        name="settings"
        options={{
          headerShown: true,
title: 'Settings',
        }}
      />
    </Stack>
  );
}
</file>

<file path="app/(app)/settings.tsx">
import { SettingsScreen } from '@/features/app/screens/settings-screen';

export default function SettingsRoute() {
  return <SettingsScreen />;
}
</file>

<file path="app/(auth)/_layout.tsx">
import { Stack } from 'expo-router';

export default function AuthLayout() {
  return <Stack screenOptions={{ headerShown: false }} />;
}
</file>

<file path="app/(auth)/register.tsx">
import { RegisterScreen } from '@/features/auth/screens/register-screen';

export default function RegisterRoute() {
  return <RegisterScreen />;
}
</file>

<file path="app/(auth)/sign-in.tsx">
import { SignInScreen } from '@/features/auth/screens/sign-in-screen';

export default function SignInRoute() {
  return <SignInScreen />;
}
</file>

<file path="app/(onboarding)/_layout.tsx">
import { Stack } from 'expo-router';

export default function OnboardingLayout() {
  return <Stack screenOptions={{ headerShown: false }} />;
}
</file>

<file path="app/(onboarding)/welcome.tsx">
import { OnboardingWelcomeScreen } from '@/features/onboarding/screens/onboarding-welcome-screen';

export default function OnboardingWelcomeRoute() {
  return <OnboardingWelcomeScreen />;
}
</file>

<file path="app/modal.tsx">
import { GlobalModalScreen } from "@/features/app/screens/global-modal-screen";

export default function ModalRoute() {
  return <GlobalModalScreen />;
}
</file>

<file path="docs/expo-router.md">
# Expo Router Foundation

Bu projede kullandigimiz route sistemi su mantiga dayanir:

## Gruplar

- `(public)`: giris gerektirmeyen ekranlar
- `(auth)`: sadece guest kullaniciya acik auth ekranlari
- `(app)`: sadece authenticated kullaniciya acik ana uygulama
- `modal.tsx`: navigation history'nin parcasi olan global modal route

## Protected Routes

Root `app/_layout.tsx` icinde `Stack.Protected` kullanilir.

- Guest ise `(public)` ve `(auth)` acik
- Authenticated ise `(app)` acik

Expo Router SDK 53+ icin resmi ve guncel pattern budur.

## Tabs

`(app)/(tabs)` altinda ana navigasyon sekmeleri tutulur.

- Kalici sekmeler: `home`, `search`, `profile`
- Route var ama sekme butonu yok: `href: null`

## Tab Bar Gizleme

Bir ekranda tab bar'i gizlemek icin o ekrani tabs grubunun disina, ama ayni app stack altina koyuyoruz.

Ornek:

- `app/(app)/(tabs)/home.tsx`
- `app/(app)/details/[id].tsx`

Bu pattern, `tabBarStyle: { display: "none" }` gibi kirilgan cozumlerden daha dogrudur.

## Modals

Iki farkli modal tipi var:

- Navigation history'de yeri olacaksa: Expo Router modal route
- Lokal popup/confirm ise: React Native `Modal`

## Sonraki Genisleme

- Gercek session persistence icin `expo-secure-store`
- Role bazli access icin nested `Stack.Protected`
- Feature flag tablari icin `Tabs.Protected`
- Deep-link map ve route helper'larinin buyutulmesi
</file>

<file path="docs/unistyles.md">
# React Native Unistyles 3.0

> Easily style cross platform React Native apps with a single StyleSheet

This documentation site is a source of truth for the good practices while building apps with React Native Unistyles.

## Documentation Sets

- [Abridged documentation](https://unistyl.es/llms-small.txt): a compact version of the documentation for React Native Unistyles 3.0, with non-essential content removed
- [Complete documentation](https://unistyl.es/llms-full.txt): the full documentation for React Native Unistyles 3.0

## Notes

- The complete documentation includes all content from the official documentation
- The content is automatically generated from the same source as the official documentation
</file>

<file path="scripts/generate-svg-icons.mjs">
import { mkdir, readdir, writeFile } from "node:fs/promises";
import path from "node:path";
import { fileURLToPath } from "node:url";

const __filename = fileURLToPath(import.meta.url);
const __dirname = path.dirname(__filename);
const projectRoot = path.resolve(__dirname, "..");
const iconsDir = path.join(projectRoot, "assets/icons/svg");
const outputFile = path.join(
projectRoot,
"src/components/ui/icon/app-icon.registry.ts",
);

const IMPORT_PREFIX = "Icon";

function toPosixPath(value) {
return value.split(path.sep).join("/");
}

function toKebabCase(value) {
return value
.replace(/\.svg$/i, "")
.replace(/([a-z0-9])([A-Z])/g, "$1-$2")
.replace(/[^a-zA-Z0-9]+/g, "-")
.replace(/^-+|-+$/g, "")
.toLowerCase();
}

function toIdentifier(value) {
const cleaned = value
.replace(/[^a-zA-Z0-9]+/g, " ")
.trim()
.split(/\s+/)
.map((part) => part.charAt(0).toUpperCase() + part.slice(1))
.join("");

return `${IMPORT_PREFIX}${cleaned || "Svg"}`;
}

async function collectSvgFiles(dir, prefix = "") {
const entries = await readdir(dir, { withFileTypes: true });
const files = await Promise.all(
entries.map(async (entry) => {
const relativePath = path.join(prefix, entry.name);
const absolutePath = path.join(dir, entry.name);

if (entry.isDirectory()) {
return collectSvgFiles(absolutePath, relativePath);
}

if (entry.isFile() && entry.name.toLowerCase().endsWith(".svg")) {
return [relativePath];
}

return [];
}),
);

return files.flat().sort((left, right) => left.localeCompare(right));
}

async function main() {
await mkdir(iconsDir, { recursive: true });

const svgFiles = await collectSvgFiles(iconsDir);

const imports = svgFiles.map((relativePath) => {
const importName = toIdentifier(relativePath);
const importPath = toPosixPath(
path.relative(
path.dirname(outputFile),
path.join(iconsDir, relativePath),
),
);

return `import ${importName} from "${importPath}";`;
});

const entries = svgFiles.map((relativePath) => {
const iconName = toKebabCase(relativePath.replaceAll(path.sep, "-"));
const importName = toIdentifier(relativePath);

return ` "${iconName}": ${importName},`;
});

const content = `// This file is auto-generated by \`npm run icons:generate\`.
// Do not edit it manually.
${imports.join("\n")}

export const appIcons = {
${entries.join("\n")}
} as const;

export type AppIconName = keyof typeof appIcons;
`;

await writeFile(outputFile, content);

console.log(
`Generated ${path.relative(projectRoot, outputFile)} with ${svgFiles.length} icon(s).`,
);
}

main().catch((error) => {
console.error(error);
process.exitCode = 1;
});
</file>

<file path="src/components/ui/app-icon.registry.ts">
// This file is auto-generated by `npm run icons:generate`.
// Do not edit it manually.
import IconAdd1Svg from "../../../assets/icons/svg/add-1.svg";
import IconAddBellNotificationNotificationAlarmAlertBellAddSvg from
"../../../assets/icons/svg/add-bell-notification--notification-alarm-alert-bell-add.svg";
import IconAddCircleButtonRemoveCrossAddButtonsPlusCircleMathematicsMathSvg from
"../../../assets/icons/svg/add-circle--button-remove-cross-add-buttons-plus-circle-+-mathematics-math.svg";
import IconAddLayer2LayerAddDesignPlusLayersSquareBoxSvg from
"../../../assets/icons/svg/add-layer-2--layer-add-design-plus-layers-square-box.svg";

export const appIcons = {
"add-1": IconAdd1Svg,
"add-bell-notification-notification-alarm-alert-bell-add": IconAddBellNotificationNotificationAlarmAlertBellAddSvg,
"add-circle-button-remove-cross-add-buttons-plus-circle-mathematics-math":
IconAddCircleButtonRemoveCrossAddButtonsPlusCircleMathematicsMathSvg,
"add-layer-2-layer-add-design-plus-layers-square-box": IconAddLayer2LayerAddDesignPlusLayersSquareBoxSvg,
} as const;

export type AppIconName = keyof typeof appIcons;
</file>

<file path="src/components/ui/app-screen.tsx">
import { PropsWithChildren, useEffect } from 'react';
import { StatusBar, StyleProp, View, ViewStyle } from 'react-native';
import { SafeAreaView } from 'react-native-safe-area-context';
import Animated, { clamp, useAnimatedScrollHandler, withTiming } from 'react-native-reanimated';
import { StyleSheet } from 'react-native-unistyles';

import { useOptionalTabBarVisibility } from '@/core/navigation/tab-bar-visibility-context';
import { useAppTheme } from '@/core/providers/theme-provider';

type AppScreenProps = PropsWithChildren<{
  contentStyle?: StyleProp<ViewStyle>;
  scrollable?: boolean;
  style?: StyleProp<ViewStyle>;
        tabBarScrollBehavior?: 'auto' | 'locked';
}>;

export function AppScreen({
  children,
  contentStyle,
  scrollable = true,
  style,
        tabBarScrollBehavior = 'auto',
}: AppScreenProps) {
  const { colorScheme } = useAppTheme();
  const tabBarVisibility = useOptionalTabBarVisibility();
        const isScrollHideEnabled = !!tabBarVisibility && !tabBarVisibility.isScrollHideLocked;

  useEffect(() => {
    if (!tabBarVisibility) {
      return;
    }

        if (tabBarScrollBehavior === 'locked') {
      tabBarVisibility.lockScrollHide();
      tabBarVisibility.translateY.value = withTiming(0, { duration: 120 });

      return () => {
        tabBarVisibility.unlockScrollHide();
      };
    }
  }, [tabBarScrollBehavior, tabBarVisibility]);

  const onScroll = useAnimatedScrollHandler({
    onBeginDrag: (event) => {
      if (!tabBarVisibility || tabBarVisibility.isScrollHideLocked) {
        return;
      }

      tabBarVisibility.previousScrollY.value = event.contentOffset.y;
    },
    onEndDrag: () => {
      if (!tabBarVisibility || tabBarVisibility.isScrollHideLocked) {
        return;
      }

        const shouldHide = tabBarVisibility.translateY.value > tabBarVisibility.tabBarHeight / 2;

      tabBarVisibility.translateY.value = withTiming(
        shouldHide ? tabBarVisibility.tabBarHeight : 0,
        { duration: 180 },
      );
    },
    onMomentumEnd: () => {
      if (!tabBarVisibility || tabBarVisibility.isScrollHideLocked) {
        return;
      }

        const shouldHide = tabBarVisibility.translateY.value > tabBarVisibility.tabBarHeight / 2;

      tabBarVisibility.translateY.value = withTiming(
        shouldHide ? tabBarVisibility.tabBarHeight : 0,
        { duration: 180 },
      );
    },
    onScroll: (event) => {
      if (!tabBarVisibility || tabBarVisibility.isScrollHideLocked) {
        return;
      }

      const currentY = Math.max(event.contentOffset.y, 0);
      const delta = currentY - tabBarVisibility.previousScrollY.value;

      tabBarVisibility.previousScrollY.value = currentY;

      if (currentY <= 0) {
        tabBarVisibility.translateY.value = withTiming(0, { duration: 120 });
        return;
      }

      tabBarVisibility.translateY.value = clamp(
        tabBarVisibility.translateY.value + delta,
        0,
        tabBarVisibility.tabBarHeight,
      );
    },
  });

  const content = (
        <View style={[styles.content, tabBarVisibility && styles.contentWithTabBar, contentStyle]}>
      {children}
    </View>
  );

  return (
    <SafeAreaView edges={['top', 'left', 'right']} style={styles.safeArea}>
    <StatusBar barStyle={colorScheme === 'dark' ? 'light-content' : 'dark-content'} />
      {scrollable ? (
        <Animated.ScrollView
          contentContainerStyle={styles.scrollContent}
          onScroll={isScrollHideEnabled ? onScroll : undefined}
          scrollEventThrottle={16}
          showsVerticalScrollIndicator={false}
    style={[styles.container, style]}>
          {content}
        </Animated.ScrollView>
      ) : (
        <View style={[styles.container, style]}>{content}</View>
      )}
    </SafeAreaView>
  );
}

const styles = StyleSheet.create((theme) => ({
  container: {
    backgroundColor: theme.colors.background,
    flex: 1,
  },
  content: {
    flex: 1,
    gap: theme.spacing.lg,
    padding: theme.spacing.xl,
  },
  contentWithTabBar: {
    paddingBottom: 96,
  },
  safeArea: {
    backgroundColor: theme.colors.background,
    flex: 1,
  },
  scrollContent: {
    flexGrow: 1,
  },
}));
</file>

<file path="src/components/ui/surface-card.tsx">
import { PropsWithChildren } from "react";
import { StyleProp, View, ViewStyle } from "react-native";
import { StyleSheet } from "react-native-unistyles";

type SurfaceCardProps = PropsWithChildren<{
  style?: StyleProp<ViewStyle>;
}>;

export function SurfaceCard({ children, style }: SurfaceCardProps) {
  return <View style={[styles.card, style]}>{children}</View>;
}

const styles = StyleSheet.create((theme) => ({
  card: {
    backgroundColor: theme.colors.surface,
    borderColor: theme.colors.border,
    borderRadius: theme.radius.lg,
    borderWidth: 1,
    gap: theme.spacing.sm,
    padding: theme.spacing.lg,
  },
}));
</file>

<file path="src/core/fonts/app-fonts.ts">
export const APP_TEXT_FONT_FAMILY = 'FigtreeVariable';

export const appFonts = {
[APP_TEXT_FONT_FAMILY]: require('../../../assets/fonts/Figtree-VariableFont_wght.ttf'),
} as const;
</file>

<file path="src/core/navigation/animated-tab-bar.tsx">
import { BottomTabBar, type BottomTabBarProps } from "@react-navigation/bottom-tabs";
import { useSafeAreaInsets } from "react-native-safe-area-context";
import Animated, {
  interpolate,
  useAnimatedStyle,
} from "react-native-reanimated";

import { useOptionalTabBarVisibility } from "./tab-bar-visibility-context";

export function AnimatedTabBar(props: BottomTabBarProps) {
  const insets = useSafeAreaInsets();
  const tabBarVisibility = useOptionalTabBarVisibility();

  const animatedStyle = useAnimatedStyle(() => ({
    opacity: interpolate(
      tabBarVisibility?.isScrollHideLocked ? 0 : tabBarVisibility?.translateY.value ?? 0,
      [0, (tabBarVisibility?.tabBarHeight ?? 72) + insets.bottom + 12],
      [1, 0.9],
    ),
    transform: [
      {
        translateY:
          tabBarVisibility?.isScrollHideLocked
            ? 0
            : tabBarVisibility?.translateY.value ?? 0,
      },
    ],
  }));

  if (!tabBarVisibility) {
    return <BottomTabBar {...props} />;
  }

  return (
    <Animated.View
      style={[
        {
          bottom: 0,
          left: 0,
          position: "absolute",
          right: 0,
        },
        animatedStyle,
      ]}
    >
      <BottomTabBar {...props} />
    </Animated.View>
  );
}
</file>

<file path="src/core/navigation/paths.ts">
import type { Href } from "expo-router";

export const paths = {
  signIn: "/sign-in",
  register: "/register",
  onboardingWelcome: "/welcome",
  appHome: "/home",
  search: "/search",
  profile: "/profile",
  settings: "/settings",
  modal: "/modal",
  details: (id: string) =>
    ({
      params: { id },
      pathname: "/details/[id]",
    }) satisfies Href,
  internalTab: "/internal",
} as const;
</file>

<file path="src/core/navigation/tab-bar-visibility-context.tsx">
import { createContext, PropsWithChildren, useContext, useMemo } from "react";
import type { SharedValue } from "react-native-reanimated";
import { useSharedValue } from "react-native-reanimated";
import { useAppStore } from "@/store";

type TabBarVisibilityContextValue = {
  isInTabs: true;
  isScrollHideLocked: boolean;
  lockScrollHide: () => void;
  unlockScrollHide: () => void;
  tabBarHeight: number;
  translateY: SharedValue<number>;
  previousScrollY: SharedValue<number>;
};

const TAB_BAR_HEIGHT = 72;

const TabBarVisibilityContext =
  createContext<TabBarVisibilityContextValue | null>(null);

export function TabBarVisibilityProvider({ children }: PropsWithChildren) {
  const isScrollHideLocked = useAppStore((state) => state.isTabBarScrollLocked);
  const lockScrollHide = useAppStore((state) => state.lockTabBarScrollHide);
  const translateY = useSharedValue(0);
  const unlockScrollHide = useAppStore((state) => state.unlockTabBarScrollHide);
  const previousScrollY = useSharedValue(0);

  const value = useMemo<TabBarVisibilityContextValue>(
    () => ({
      isInTabs: true,
      isScrollHideLocked,
      lockScrollHide,
      unlockScrollHide,
      previousScrollY,
      tabBarHeight: TAB_BAR_HEIGHT,
      translateY,
    }),
    [
      isScrollHideLocked,
      lockScrollHide,
      previousScrollY,
      translateY,
      unlockScrollHide,
    ],
  );

  return (
    <TabBarVisibilityContext.Provider value={value}>
      {children}
    </TabBarVisibilityContext.Provider>
  );
}

export function useOptionalTabBarVisibility() {
  return useContext(TabBarVisibilityContext);
}
</file>

<file path="src/core/providers/app-providers.tsx">
import { PropsWithChildren, useEffect } from "react";
import { AppState } from "react-native";
import { SafeAreaProvider } from "react-native-safe-area-context";

import "@/i18n";

import { useAppStore } from "@/store";

import { LoginSheetProvider } from "./login-sheet-provider";
import { AppQueryProvider } from "./query-provider";
import { ThemeProvider } from "./theme-provider";

export function AppProviders({ children }: PropsWithChildren) {
  const syncDeviceLocale = useAppStore((state) => state.syncDeviceLocale);

  useEffect(() => {
    const subscription = AppState.addEventListener("change", (nextState) => {
      if (nextState === "active") {
        syncDeviceLocale();
      }
    });

    return () => {
      subscription.remove();
    };
  }, [syncDeviceLocale]);

  return (
    <SafeAreaProvider>
      <AppQueryProvider>
          <ThemeProvider>
              <LoginSheetProvider>{children}</LoginSheetProvider>
          </ThemeProvider>
      </AppQueryProvider>
    </SafeAreaProvider>
  );
}
</file>

<file path="src/core/providers/query-provider.tsx">
import { PropsWithChildren } from "react";
import { QueryClientProvider } from "@tanstack/react-query";

import { queryClient } from "@/services/api/query-client";

export function AppQueryProvider({ children }: PropsWithChildren) {
  return <QueryClientProvider client={queryClient}>{children}</QueryClientProvider>;
}
</file>

<file path="src/core/providers/theme-provider.tsx">
import {
  createContext,
  PropsWithChildren,
  useContext,
  useMemo,
} from "react";

import {
  darkTheme,
  lightTheme,
  type AppTheme,
  type ThemePreference,
} from "@/theme";
import { useAppStore } from "@/store/app-store";

type ThemeContextValue = {
  theme: AppTheme;
  colorScheme: ThemePreference;
  setColorScheme: (scheme: ThemePreference) => void;
};

const ThemeContext = createContext<ThemeContextValue | null>(null);

export function ThemeProvider({ children }: PropsWithChildren) {
  const themePreference = useAppStore((state) => state.themePreference);
  const setThemePreference = useAppStore((state) => state.setThemePreference);
  const theme = themePreference === "dark" ? darkTheme : lightTheme;
  const colorScheme = themePreference;

  const value = useMemo<ThemeContextValue>(
    () => ({
      theme,
      colorScheme,
      setColorScheme: setThemePreference,
    }),
    [colorScheme, setThemePreference, theme],
  );

  return <ThemeContext.Provider value={value}>{children}</ThemeContext.Provider>;
}

export function useAppTheme() {
  const context = useContext(ThemeContext);

  if (!context) {
    throw new Error("useAppTheme must be used within ThemeProvider.");
  }

  return context;
}
</file>

<file path="src/core/providers/theme-scope.tsx">
import { PropsWithChildren, useEffect } from "react";
import { View } from "react-native";
import { StyleSheet, UnistylesRuntime } from "react-native-unistyles";

import { useAppTheme } from "./theme-provider";

export function ThemeScope({ children }: PropsWithChildren) {
  const { theme } = useAppTheme();

  useEffect(() => {
    UnistylesRuntime.setTheme(theme.id);
    UnistylesRuntime.setRootViewBackgroundColor(theme.colors.background);
  }, [theme]);

  return <View style={styles.root}>{children}</View>;
}

const styles = StyleSheet.create((theme) => ({
  root: {
    flex: 1,
    backgroundColor: theme.colors.background,
  },
}));
</file>

<file path="src/features/app/screens/details-screen.tsx">
import { useLocalSearchParams, router } from "expo-router";
import { StyleSheet } from "react-native-unistyles";

import { AppScreen, Button, Surface, Typography } from "@/components/ui";

export function DetailsScreen() {
  const { id } = useLocalSearchParams<{ id: string }>();

  return (
    <AppScreen>
        <Surface style={styles.card}>
        <Typography variant="label">Stack Sibling</Typography>
        <Typography variant="title">Details {id}</Typography>
        <Typography color="muted">
          Bu ekran tabs grubunun disinda tanimlandigi icin tab bar otomatik
          olarak gizlenir. Detay, checkout, onboarding-step gibi tam ekran
          akislarda en dogru pattern bu.
        </Typography>
        <Button
          label="Geri Don"
          onPress={() => router.back()}
          style={styles.fullWidth}
        variant="surface"
        />
    </Surface>
    </AppScreen>
  );
}

const styles = StyleSheet.create((theme) => ({
  card: {
    borderRadius: theme.radius.xl,
  },
  fullWidth: {
    alignSelf: "stretch",
  },
}));
</file>

<file path="src/features/app/screens/global-modal-screen.tsx">
import { router } from "expo-router";
import { StyleSheet } from "react-native-unistyles";

import { AppScreen, Button, Surface, Typography } from "@/components/ui";

export function GlobalModalScreen() {
  return (
    <AppScreen contentStyle={styles.centeredContent}>
<Surface style={styles.card}>
<Typography variant="label">Modal Route</Typography>
<Typography variant="title">Global Modal</Typography>
<Typography color="muted">
          Kisa ama navigation historynin parcasi olmasi gereken akislarda Expo
          Router modal routeu kullanilir. Basit confirm/popup senaryolari icin
          ise React Native `Modal` daha dogrudur.
</Typography>
<Button
          label="Modal Kapat"
          onPress={() => router.back()}
          style={styles.fullWidth}
variant="primary"
        />
</Surface>
    </AppScreen>
  );
}

const styles = StyleSheet.create((theme) => ({
  card: {
    borderRadius: theme.radius.xl,
  },
  centeredContent: {
    justifyContent: "center",
  },
  fullWidth: {
    alignSelf: "stretch",
  },
}));
</file>

<file path="src/features/app/screens/internal-route-screen.tsx">
import { AppScreen, Surface, Typography } from "@/components/ui";
import { StyleSheet } from "react-native-unistyles";

export function InternalRouteScreen() {
  return (
    <AppScreen>
        <Surface style={styles.card}>
        <Typography variant="label">Hidden Tab</Typography>
        <Typography variant="title">Internal Route</Typography>
        <Typography color="muted">
          Bu route tabs icinde yasiyor ama tab barda gorunmuyor. Expo docstaki
          `href: null` paterni bunun icin en temiz yontem.
        </Typography>
    </Surface>
    </AppScreen>
  );
}

const styles = StyleSheet.create((theme) => ({
  card: {
    borderRadius: theme.radius.xl,
  },
}));
</file>

<file path="src/features/app/screens/profile-screen.tsx">
import { router } from 'expo-router';
import { StyleSheet } from 'react-native-unistyles';

import { AppScreen, Button, Icon, Surface, Typography } from '@/components/ui';
import { paths } from '@/core/navigation/paths';
import { useAppTranslation, useLocaleSettings } from '@/i18n';
import { useAppStore } from '@/store';
import { useSession } from '@/features/auth';
import { useAppTheme } from '@/core/providers/theme-provider';
import { View } from 'react-native';

export function ProfileScreen() {
const { t } = useAppTranslation();
const colorScheme = useAppStore((state) => state.themePreference);
const setLocaleOverride = useAppStore((state) => state.setLocaleOverride);
const toggleThemePreference = useAppStore((state) => state.toggleThemePreference);
const { direction, locale, isRTL } = useLocaleSettings();
const { signOut } = useSession();
const { theme } = useAppTheme();

return (
<AppScreen tabBarScrollBehavior="locked">
    <Surface style={styles.card}>
    <Typography variant="label">Protected App</Typography>
    <Typography variant="title">Home Tab</Typography>
    <Typography color="muted">
        Burasi auth gerektiren ana uygulama alani. Tabs burada yasar. Tab bari gizlemek
        istedigimiz detay ekranlari ise tabs grubunun disinda, ayni stack icinde sibling route
        olarak tanimlanir.
    </Typography>
</Surface>
<Icon name="add-1" size={48} color={theme.colors.primary} />
<Surface>
    <Typography variant="headline">{t('settings.languageSectionTitle')}</Typography>
    <View style={[styles.languageButtons, isRTL ? styles.justifyEnd : styles.justifyStart]}>
    {(['en', 'tr', 'ar'] as const).map((language) => {
    const isActive = locale === language;

    return (
    <Button
            key={language}
    label={t(`settings.languageNames.${language}`)}
    onPress={() => setLocaleOverride(language)}
    style={styles.languageButton}
    variant={isActive ? 'primary' : 'surface'}
    />
    );
    })}
</View>
<Typography color="muted">
    Locale: {locale.toUpperCase()} | Direction: {direction.toUpperCase()}
</Typography>
</Surface>

<Surface>
<Typography variant="headline">{t('settings.themeSectionTitle')}</Typography>
<View style={styles.actions}>
<Typography color="muted">Theme: {colorScheme === 'light' ? 'LIGHT' : 'DARK'}</Typography>
<Button
        label={
colorScheme === 'light' ? t('settings.switchToDark') : t('settings.switchToLight')
}
onPress={toggleThemePreference}
style={styles.fullWidth}
variant="secondary"
/>
</View>
        </Surface>

<Surface>
<Typography variant="headline">Navigation Patterns</Typography>
<View style={styles.actions}>
<Button
        label="Details Ekranina Git (tab bar gizlenir)"
        onPress={() => router.push(paths.details('42'))}
style={styles.fullWidth}
variant="primary"
/>
<Button
        label="Settings Ekrani"
        onPress={() => router.push(paths.settings)}
style={styles.fullWidth}
variant="surface"
/>
<Button
        label="Global Modal"
        onPress={() => router.push(paths.modal)}
style={styles.fullWidth}
variant="surface"
/>
<Button
        label="Hidden Tab Routea Git"
        onPress={() => router.push(paths.internalTab)}
style={styles.fullWidth}
variant="surface"
/>
<Button
        label="Sign Out"
        onPress={async () => {
await signOut();
router.replace(paths.signIn);
}}
style={styles.fullWidth}
variant="secondary"
/>
</View>
        </Surface>
    </AppScreen>
  );
}

const styles = StyleSheet.create((theme) => ({
        actions: {
        gap: theme.spacing.sm,
        },
  card: {
    borderRadius: theme.radius.xl,
  },
  fullWidth: {
        alignSelf: 'stretch',
        },
        justifyEnd: {
        justifyContent: 'flex-end',
        },
        justifyStart: {
        justifyContent: 'flex-start',
        },
        languageButton: {
        minWidth: 96,
        },
        languageButtons: {
        flexDirection: 'row',
        flexWrap: 'wrap',
        gap: theme.spacing.xxs,
  },
}));
</file>

<file path="src/features/app/screens/search-screen.tsx">
import { AppScreen, Surface, Typography } from "@/components/ui";
import { StyleSheet } from "react-native-unistyles";

export function SearchScreen() {
  return (
    <AppScreen>
        <Surface style={styles.card}>
        <Typography variant="label">Tab</Typography>
        <Typography variant="title">Search</Typography>
        <Typography color="muted">
          Search gibi ana navigasyon ekranlari tabs altinda kalir. Buraya kendi
          nested stackin de eklenebilir.
        </Typography>
    </Surface>
    </AppScreen>
  );
}

const styles = StyleSheet.create((theme) => ({
  card: {
    borderRadius: theme.radius.xl,
  },
}));
</file>

<file path="src/features/app/screens/settings-screen.tsx">
import { AppScreen, Surface, Typography } from "@/components/ui";
import { StyleSheet } from "react-native-unistyles";

export function SettingsScreen() {
  return (
    <AppScreen>
        <Surface style={styles.card}>
        <Typography variant="label">Stack Screen</Typography>
        <Typography variant="title">Settings</Typography>
        <Typography color="muted">
          Tabs disindaki ama modal olmayan ikincil ekranlar bu seviyede
          tutulabilir. Bu sayede header, gestures ve transition ayarlari stack
          bazinda yonetilir.
        </Typography>
    </Surface>
    </AppScreen>
  );
}

const styles = StyleSheet.create((theme) => ({
  card: {
    borderRadius: theme.radius.xl,
  },
}));
</file>

<file path="src/features/auth/hooks/use-session.ts">
import { useSessionStore } from '@/features/auth/store/session-store';

export function useSession() {
  const completeOnboarding = useSessionStore((state) => state.completeOnboarding);
const hasCompletedOnboarding = useSessionStore((state) => state.hasCompletedOnboarding);
  const status = useSessionStore((state) => state.status);
  const sessionToken = useSessionStore((state) => state.sessionToken);
  const bootstrap = useSessionStore((state) => state.bootstrap);
  const register = useSessionStore((state) => state.register);
  const signIn = useSessionStore((state) => state.signIn);
  const signOut = useSessionStore((state) => state.signOut);

  return {
    bootstrap,
    completeOnboarding,
    hasCompletedOnboarding,
isAuthenticated: status === 'authenticated',
isLoading: status === 'checking',
    register,
    sessionToken,
    signIn,
    signOut,
    status,
  };
}
</file>

<file path="src/features/auth/index.ts">
export { useSession } from './hooks/use-session';
export { sessionStore, useSessionStore } from './store/session-store';
</file>

<file path="src/features/home/screens/home-screen.tsx">
import { View } from "react-native";
import { StyleSheet } from "react-native-unistyles";

import { AppScreen, Button, Surface, Typography } from "@/components/ui";
import { useAppTranslation } from "@/i18n";
import { useAppStore } from "@/store/app-store";

export function HomeScreen() {
  const { t } = useAppTranslation();
  const localeOverride = useAppStore((state) => state.localeOverride);
  const setLocaleOverride = useAppStore((state) => state.setLocaleOverride);

  const architectureItems = t("home.architectureItems", {
    returnObjects: true,
  }) as string[];

  return (
    <AppScreen>
      <View style={styles.heroSection}>
        <Typography variant="label">{t("home.badge")}</Typography>
        <Typography variant="title">{t("home.title")}</Typography>
        <Typography variant="bodyMuted">{t("home.description")}</Typography>
        <Button
          label={`${t("common.continue")} (${localeOverride ?? "system"})`}
          onPress={() =>
            setLocaleOverride(localeOverride === "tr" ? "en" : "tr")
          }
        />
      </View>

<Surface>
    <Typography variant="headline">{t("home.architectureTitle")}</Typography>
        {architectureItems.map((item) => (
    <Typography key={item} variant="bodyMuted">
            • {item}
</Typography>
        ))}
</Surface>

<Surface>
<Typography variant="headline">{t("home.nextTitle")}</Typography>
<Typography variant="bodyMuted">{t("home.nextDescription")}</Typography>
</Surface>
    </AppScreen>
  );
}

const styles = StyleSheet.create((theme) => ({
  heroSection: {
    backgroundColor: theme.colors.surfaceMuted,
    borderColor: theme.colors.accent,
    borderRadius: theme.radius.xl,
    borderWidth: 1,
    gap: theme.spacing.md,
    padding: theme.spacing.xl,
  },
}));
</file>

<file path="src/features/public/screens/public-home-screen.tsx">
import { router } from "expo-router";
import { View } from "react-native";
import { StyleSheet } from "react-native-unistyles";

import { AppScreen, Button, Surface, Typography } from "@/components/ui";
import { paths } from "@/core/navigation/paths";

export function PublicHomeScreen() {
  return (
    <AppScreen contentStyle={styles.centeredContent}>
<Surface style={styles.heroCard}>
<Typography variant="label">Public</Typography>
<Typography variant="title">Expo Router Foundation</Typography>
<Typography color="muted">
          Public sayfalar giris gerektirmez. Burasi onboarding, landing, legal
          veya marketing ekranlari icin dogru grup.
</Typography>
        <View style={styles.actions}>
<Button
            label="Sign In Flow'a Git"
            onPress={() => router.push(paths.signIn)}
            style={styles.fullWidth}
variant="primary"
          />
<Button
            label="Global Modal Ac"
            onPress={() => router.push(paths.modal)}
            style={styles.fullWidth}
variant="surface"
          />
        </View>
        </Surface>
    </AppScreen>
  );
}

const styles = StyleSheet.create((theme) => ({
  actions: {
    gap: theme.spacing.sm,
  },
  centeredContent: {
    justifyContent: "center",
  },
  fullWidth: {
    alignSelf: "stretch",
  },
  heroCard: {
    borderRadius: theme.radius.xl,
    gap: theme.spacing.md,
  },
}));
</file>

<file path="src/i18n/index.ts">
import { createInstance } from 'i18next';
import { initReactI18next, useTranslation } from 'react-i18next';

import { appI18nStore } from '@/store/app-store';
import { useAppStore } from '@/store';

import {
  detectDeviceLocale,
  getLocaleDirection,
  isRTLLocale,
  isSupportedLocale,
  type Locale,
} from './locale';
import { ar, en, tr } from './translations';

const resources = {
  ar: {
    translation: ar,
  },
  en: {
    translation: en,
  },
  tr: {
    translation: tr,
  },
} as const;

const i18n = createInstance();

function resolveInitialLocale(): Locale {
  const override = appI18nStore.getState().localeOverride;

  if (override && isSupportedLocale(override)) {
    return override;
  }

  return detectDeviceLocale();
}

void i18n.use(initReactI18next).init({
compatibilityJSON: 'v4',
  resources,
  lng: resolveInitialLocale(),
fallbackLng: 'en',
  interpolation: {
    escapeValue: false,
  },
  returnNull: false,
});

appI18nStore.subscribe((state, previousState) => {
  if (state.localeOverride !== previousState.localeOverride) {
    void i18n.changeLanguage(state.localeOverride ?? detectDeviceLocale());
  }
});

export { i18n };
export type { Locale } from './locale';
export { detectDeviceLocale, getLocaleDirection, isRTLLocale, isSupportedLocale } from './locale';
export function useAppTranslation() {
  return useTranslation();
}

export function useLocaleSettings() {
  const locale = useAppStore((state) => state.resolvedLocale);
  const isRTL = isRTLLocale(locale);

  return {
    locale,
    direction: getLocaleDirection(locale),
    isRTL,
  };
}
</file>

<file path="src/i18n/locale.ts">
import { getLocales } from 'expo-localization';

const supportedLocales = ['en', 'tr', 'ar'] as const;
const rtlLocales = ['ar'] as const;

export type Locale = (typeof supportedLocales)[number];
export type LocaleDirection = 'ltr' | 'rtl';

export function isSupportedLocale(value: string): value is Locale {
  return supportedLocales.includes(value as Locale);
}

export function isRTLLocale(locale: Locale) {
  return rtlLocales.includes(locale as (typeof rtlLocales)[number]);
}

export function getLocaleDirection(locale: Locale): LocaleDirection {
return isRTLLocale(locale) ? 'rtl' : 'ltr';
}

export function detectDeviceLocale(): Locale {
const localeTag = getLocales()[0]?.languageCode?.toLowerCase() ?? 'en';

  if (isSupportedLocale(localeTag)) {
    return localeTag;
  }

return 'en';
}

export function detectDeviceDirection(): LocaleDirection {
  return getLocaleDirection(detectDeviceLocale());
}
</file>

<file path="src/services/api/api-client.ts">
import axios from 'axios';

import { getAccessToken } from '@/services/storage/secure-storage';

import { normalizeApiError } from './normalize-api-error';

export const apiClient = axios.create({
  baseURL: process.env.EXPO_PUBLIC_BASE_URL,
  timeout: 10000,
  headers: {
'Content-Type': 'application/json',
  },
});

apiClient.interceptors.request.use(async (config) => {
const token = await getAccessToken();

if (token) {
config.headers.Authorization = `Bearer ${token}`;
}

return config;
});

apiClient.interceptors.response.use(
  (response) => response,
  (error) => Promise.reject(normalizeApiError(error)),
);
</file>

<file path="src/services/api/index.ts">
export { apiClient } from './api-client';
export { normalizeApiError } from './normalize-api-error';
export { queryClient } from './query-client';
export { queryKeys } from './query-keys';
</file>

<file path="src/services/api/normalize-api-error.ts">
import { isAxiosError } from "axios";

export type ApiError = {
  message: string;
  status?: number;
  code?: string;
};

export function normalizeApiError(error: unknown): ApiError {
  if (isAxiosError(error)) {
    return {
      message: error.response?.data?.message ?? error.message ?? "Request failed.",
      status: error.response?.status,
      code: error.code,
    };
  }

  if (error instanceof Error) {
    return {
      message: error.message,
    };
  }

  return {
    message: "Unknown error.",
  };
}
</file>

<file path="src/services/api/query-client.ts">
import { QueryClient } from '@tanstack/react-query';

export const queryClient = new QueryClient({
  defaultOptions: {
    queries: {
      retry: 1,
      refetchOnWindowFocus: false,
      staleTime: 30_000,
    },
    mutations: {
      retry: 0,
    },
  },
});
</file>

<file path="src/services/api/query-keys.ts">
import type { GetJobsParams, PaginationParams } from '@/services/jobs/types';
import type { NearbyParams } from '@/services/profile/types';

export const queryKeys = {
  app: {
root: ['app'] as const,
},
auth: {
all: ['auth'] as const,
me: () => [...queryKeys.auth.all, 'me'] as const,
verifyEmail: (token: string | null) =>
[...queryKeys.auth.all, 'verify-email', token] as const,
},
jobs: {
all: ['jobs'] as const,
list: (params?: GetJobsParams) => [...queryKeys.jobs.all, 'list', params] as const,
infinite: (params?: Omit<GetJobsParams, 'page' | 'limit'> & { initialLimit?: number }) =>
[...queryKeys.jobs.all, 'infinite', params] as const,
detail: (id: string) => [...queryKeys.jobs.all, 'detail', id] as const,
my: (params?: PaginationParams) => [...queryKeys.jobs.all, 'my', params] as const,
applications: (jobId: string) => [...queryKeys.jobs.all, jobId, 'applications'] as const,
myApplications: (params?: PaginationParams) =>
[...queryKeys.jobs.all, 'my-applications', params] as const,
},
payment: {
all: ['payment'] as const,
creditPlans: () => [...queryKeys.payment.all, 'credit-plans'] as const,
},
profile: {
all: ['profile'] as const,
detail: () => [...queryKeys.profile.all, 'detail'] as const,
nearby: (params: NearbyParams) => [...queryKeys.profile.all, 'nearby', params] as const,
  },
} as const;
</file>

<file path="src/services/storage/secure-storage.ts">
import * as SecureStore from 'expo-secure-store';

const ACCESS_TOKEN_KEY = 'auth.access-token';
const REFRESH_TOKEN_KEY = 'auth.refresh-token';

export async function getAccessToken() {
  return SecureStore.getItemAsync(ACCESS_TOKEN_KEY);
}

export async function setAccessToken(token: string) {
  await SecureStore.setItemAsync(ACCESS_TOKEN_KEY, token);
}

export async function removeAccessToken() {
  await SecureStore.deleteItemAsync(ACCESS_TOKEN_KEY);
}

export async function getRefreshToken() {
return SecureStore.getItemAsync(REFRESH_TOKEN_KEY);
}

export async function setRefreshToken(token: string) {
await SecureStore.setItemAsync(REFRESH_TOKEN_KEY, token);
}

export async function removeRefreshToken() {
await SecureStore.deleteItemAsync(REFRESH_TOKEN_KEY);
}

export async function clearAuthTokens() {
await removeAccessToken();
await removeRefreshToken();
}
</file>

<file path="src/store/index.ts">
export { appI18nStore, useAppStore } from "./app-store";
</file>

<file path="src/store/storage.ts">
import { createMMKV } from "react-native-mmkv";
import type { StateStorage } from "zustand/middleware";

const storage = createMMKV({
id: "ae-mobil-storage",
});

export const mmkvStateStorage: StateStorage = {
  getItem: (name) => storage.getString(name) ?? null,
  removeItem: (name) => {
    storage.remove(name);
  },
  setItem: (name, value) => {
    storage.set(name, value);
  },
};
</file>

<file path="src/theme/index.ts">
export { darkTheme, lightTheme } from './tokens';
export type { AppTheme, ThemePreference } from './tokens';
</file>

<file path="src/theme/unistyles.ts">
import { StyleSheet } from 'react-native-unistyles';

import { darkTheme, lightTheme } from './tokens';

export const appThemes = {
  light: lightTheme,
  dark: darkTheme,
} as const;

StyleSheet.configure({
  settings: {
initialTheme: 'light',
  },
  themes: appThemes,
});
</file>

<file path="src/types/svg.d.ts">
declare module "*.svg" {
import type { FC, MemoExoticComponent } from "react";
import type { SvgProps } from "react-native-svg";

const content: MemoExoticComponent
<FC
<SvgProps>>;
export default content;
}
</file>

<file path=".svgrrc">
{
"native": true,
"typescript": true,
"memo": true,
"dimensions": false,
"expandProps": "end"
}
</file>

<file path=".yarnrc.yml">
nodeLinker: node-modules
</file>

<file path="babel.config.js">
module.exports = function (api) {
  api.cache(true);

  return {
    presets: ["babel-preset-expo"],
    plugins: [["react-native-unistyles/plugin", { root: "src" }], "react-native-reanimated/plugin"],
  };
};
</file>

<file path="index.js">
import "@/theme/unistyles";
import "expo-router/entry";
</file>

<file path="skills-lock.json">
{
  "version": 1,
  "skills": {
"building-native-ui": {
"source": "expo/skills",
"sourceType": "github",
"skillPath": "plugins/expo/skills/building-native-ui/SKILL.md",
"computedHash": "342df93f481a0dba919f372d6c7b40d2b4bf5b51dd24363aea2e5d0bae27a6fa"
},
"eas-update-insights": {
"source": "expo/skills",
"sourceType": "github",
"skillPath": "plugins/expo/skills/eas-update-insights/SKILL.md",
"computedHash": "1e75ed22734fb37993f0ae0c57eceeaf37661ab452d7c7f55c740b1e41c2a0c1"
},
"expo-api-routes": {
"source": "expo/skills",
"sourceType": "github",
"skillPath": "plugins/expo/skills/expo-api-routes/SKILL.md",
"computedHash": "015c6b849507fda73fcc32d2448f033aaaaa21f5229085342b8421727a90cafb"
},
"expo-deployment": {
"source": "expo/skills",
"sourceType": "github",
"skillPath": "plugins/expo/skills/expo-deployment/SKILL.md",
"computedHash": "9ea9f16374765c1b16764a51bd43a64098921b33f48e94d9c5c1cce24b335c10"
},
"expo-dev-client": {
"source": "expo/skills",
"sourceType": "github",
"skillPath": "plugins/expo/skills/expo-dev-client/SKILL.md",
"computedHash": "234e2633b7fbcef2d479f8fe8ab20d53d08ed3e4beec7c965da4aff5b43affe7"
},
"expo-module": {
"source": "expo/skills",
"sourceType": "github",
"skillPath": "plugins/expo/skills/expo-module/SKILL.md",
"computedHash": "c1e47893d207b68c9a2a5b0de3f97042f82a65c0b3c3a33a7114029211b8e89b"
},
    "react-native-best-practices": {
      "source": "callstackincubator/agent-skills",
      "sourceType": "github",
      "computedHash": "bad5e73a1c3ddca2ed4bcd93b05ff913e8f9cc5495feacc3717c69e07fee540f"
    },
"upgrading-expo": {
"source": "expo/skills",
"sourceType": "github",
"skillPath": "plugins/expo/skills/upgrading-expo/SKILL.md",
"computedHash": "939a6820e0d6ead943bb4ad93cec647815e73fbc04043401773b2138e9e13b69"
},
    "vercel-react-native-skills": {
      "source": "vercel-labs/agent-skills",
      "sourceType": "github",
      "computedHash": "41d24eafa7c3d82e270439808f7cfbc4d51aeb2d14f2809a2267c16275784d06"
    }
  }
}
</file>

<file path="unistyles.d.ts">
import type { appThemes } from "@/theme/unistyles";

type AppThemes = typeof appThemes;

declare module "react-native-unistyles" {
  export interface UnistylesThemes extends AppThemes {}
}
</file>

<file path="src/components/ui/app-text.tsx">
import { ReactNode } from "react";
import { Platform, StyleProp, Text, TextStyle } from "react-native";
import { StyleSheet } from "react-native-unistyles";

import { useLocaleSettings } from "@/i18n";

type AppTextVariant = "body" | "bodyMuted" | "title" | "headline" | "label";
type AppTextColor =
  | "default"
  | "muted"
  | "primary"
  | "primaryForeground"
  | "success"
  | "warning"
  | "danger"
  | "info";

type AppTextProps = {
  children: ReactNode;
  color?: AppTextColor;
  style?: StyleProp<TextStyle>;
  variant?: AppTextVariant;
};

export function AppText({
  children,
  color = "default",
  style,
  variant = "body",
}: AppTextProps) {
  const { direction, isRTL, locale } = useLocaleSettings();
  const webDirectionProps =
    Platform.OS === "web" ? { dir: direction, lang: locale } : undefined;

  return (
    <Text
      style={[
        styles[variant],
        styles[color],
        isRTL ? styles.alignRight : styles.alignLeft,
        style,
      ]}
      {...webDirectionProps}
    >
      {children}
    </Text>
  );
}

const styles = StyleSheet.create((theme) => ({
  alignLeft: {
    textAlign: "left",
  },
  alignRight: {
    textAlign: "right",
  },
  body: {
    color: theme.colors.text,
    fontFamily: theme.typography.fontFamily.regular,
    fontSize: theme.typography.fontSize.md,
    lineHeight: theme.typography.lineHeight.md,
  },
  bodyMuted: {
    color: theme.colors.textMuted,
    fontFamily: theme.typography.fontFamily.regular,
    fontSize: theme.typography.fontSize.md,
    lineHeight: theme.typography.lineHeight.md,
  },
  default: {},
  danger: {
    color: theme.colors.danger,
  },
  headline: {
    color: theme.colors.text,
    fontFamily: theme.typography.fontFamily.semibold,
    fontSize: theme.typography.fontSize.lg,
    fontWeight: "600",
    lineHeight: theme.typography.lineHeight.lg,
  },
  info: {
    color: theme.colors.info,
  },
  label: {
    color: theme.colors.primary,
    fontFamily: theme.typography.fontFamily.semibold,
    fontSize: theme.typography.fontSize.sm,
    fontWeight: "600",
    letterSpacing: 0.4,
    lineHeight: theme.typography.lineHeight.sm,
    textTransform: "uppercase",
  },
  muted: {
    color: theme.colors.textMuted,
  },
  primary: {
    color: theme.colors.primary,
  },
  primaryForeground: {
    color: theme.colors.primaryForeground,
  },
  success: {
    color: theme.colors.success,
  },
  title: {
    color: theme.colors.text,
    fontFamily: theme.typography.fontFamily.bold,
    fontSize: theme.typography.fontSize.xxl,
    fontWeight: "700",
    lineHeight: theme.typography.lineHeight.xxl,
  },
  warning: {
    color: theme.colors.warning,
  },
}));
</file>

<file path="src/components/ui/index.ts">
export type { AppIconName, AppIconProps } from './icon';
export {
Button,
ChipButton,
IconButton,
TabButton,
type ButtonHaptic,
type ButtonIcon,
type ButtonSize,
type ButtonVariant,
type ChipButtonSize,
type ChipButtonVariant,
type IconButtonSize,
type IconButtonVariant,
type TabButtonSize,
} from './button';
export { AppScreen } from './app-screen';
export { Icon } from './icon';
export { Surface } from './surface';
export { Typography } from './typography';
</file>

<file path="src/features/auth/screens/register-screen.tsx">
import { router } from 'expo-router';
import { View } from 'react-native';
import { StyleSheet } from 'react-native-unistyles';

import { AppScreen, Button, Surface, Typography } from '@/components/ui';
import { paths } from '@/core/navigation/paths';

import { useSession } from '../hooks/use-session';

export function RegisterScreen() {
  const { register } = useSession();

  return (
    <AppScreen contentStyle={styles.centeredContent}>
<Surface style={styles.heroCard}>
<Typography variant="label">Auth</Typography>
<Typography variant="title">Register</Typography>
<Typography color="muted">
    Yeni kullanici register oldugunda session olusur ama onboarding henüz tamamlanmadigi icin
    route guard onu onboarding akisina gonderir.
</Typography>
        <View style={styles.actions}>
<Button
            label="Demo Register"
            onPress={async () => {
              await register();
              router.replace(paths.onboardingWelcome);
            }}
            style={styles.fullWidth}
variant="primary"
          />
<Button
            label="Sign In'a Don"
            onPress={() => router.replace(paths.signIn)}
            style={styles.fullWidth}
variant="surface"
          />
        </View>
        </Surface>
    </AppScreen>
  );
}

const styles = StyleSheet.create((theme) => ({
  actions: {
    gap: theme.spacing.sm,
  },
  centeredContent: {
        justifyContent: 'center',
  },
  fullWidth: {
        alignSelf: 'stretch',
  },
  heroCard: {
    borderRadius: theme.radius.xl,
    gap: theme.spacing.md,
  },
}));
</file>

<file path="src/features/auth/screens/sign-in-screen.tsx">
import { router } from "expo-router";
import { View } from "react-native";
import { StyleSheet } from "react-native-unistyles";

import { AppScreen, Button, Surface, Typography } from "@/components/ui";
import { paths } from "@/core/navigation/paths";

import { useSession } from "../hooks/use-session";

export function SignInScreen() {
  const { signIn } = useSession();

  return (
    <AppScreen contentStyle={styles.centeredContent}>
<Surface style={styles.heroCard}>
<Typography variant="label">Auth</Typography>
<Typography variant="title">Sign In</Typography>
<Typography color="muted">
          Auth ekranlari ayri route group altinda tutulur. Kullanici giris
          yaptiginda route guard onu dogru yere yonlendirir. Mevcut user ise ana
          uygulamaya, onboarding eksikse onboarding akisina gider.
</Typography>
        <View style={styles.actions}>
<Button
            label="Mevcut Kullanici Sign In"
            onPress={async () => {
              await signIn();
              router.replace(paths.appHome);
            }}
            style={styles.fullWidth}
variant="primary"
          />
<Button
            label="Register'a Git"
            onPress={() => router.push(paths.register)}
            style={styles.fullWidth}
variant="surface"
          />
<Button
        label="Ana Sayfaya Git"
        onPress={() => router.replace(paths.appHome)}
            style={styles.fullWidth}
variant="surface"
          />
        </View>
        </Surface>
    </AppScreen>
  );
}

const styles = StyleSheet.create((theme) => ({
  actions: {
    gap: theme.spacing.sm,
  },
  centeredContent: {
    justifyContent: "center",
  },
  fullWidth: {
    alignSelf: "stretch",
  },
  heroCard: {
    borderRadius: theme.radius.xl,
    gap: theme.spacing.md,
  },
}));
</file>

<file path="src/features/onboarding/screens/onboarding-welcome-screen.tsx">
import { router } from "expo-router";
import { View } from "react-native";
import { StyleSheet } from "react-native-unistyles";

import { AppScreen, Button, Surface, Typography } from "@/components/ui";
import { paths } from "@/core/navigation/paths";
import { useSession } from "@/features/auth";

export function OnboardingWelcomeScreen() {
  const { completeOnboarding, signOut } = useSession();

  return (
    <AppScreen contentStyle={styles.centeredContent} tabBarScrollBehavior="locked">
<Surface style={styles.heroCard}>
<Typography variant="label">Onboarding</Typography>
<Typography variant="title">Hos Geldin</Typography>
<Typography color="muted">
          Bu alan register sonrasi ilk kurulumu temsil eder. Profil tamamlama,
          ilgi alani secimi, izin onboardingi gibi akislari burada toplariz.
</Typography>
        <View style={styles.actions}>
<Button
            label="Onboarding'i Tamamla"
            onPress={() => {
              completeOnboarding();
              router.replace(paths.appHome);
            }}
            style={styles.fullWidth}
variant="primary"
          />
<Button
            label="Cikisa Don"
            onPress={async () => {
              await signOut();
router.replace(paths.onboardingWelcome);
            }}
            style={styles.fullWidth}
variant="surface"
          />
        </View>
        </Surface>
    </AppScreen>
  );
}

const styles = StyleSheet.create((theme) => ({
  actions: {
    gap: theme.spacing.sm,
  },
  centeredContent: {
    justifyContent: "center",
  },
  fullWidth: {
    alignSelf: "stretch",
  },
  heroCard: {
    borderRadius: theme.radius.xl,
    gap: theme.spacing.md,
  },
}));
</file>

<file path="src/store/app-store.ts">
import { createStore, useStore } from "zustand";
import { createJSONStorage, persist } from "zustand/middleware";

import { detectDeviceLocale, type Locale } from "@/i18n/locale";
import type { ThemePreference } from "@/theme";

import { mmkvStateStorage } from "./storage";

type AppState = {
  isTabBarScrollLocked: boolean;
  localeOverride: Locale | null;
  resolvedLocale: Locale;
  themePreference: ThemePreference;
};

type AppActions = {
  setLocaleOverride: (locale: Locale) => void;
  clearLocaleOverride: () => void;
  lockTabBarScrollHide: () => void;
  setThemePreference: (theme: ThemePreference) => void;
  toggleThemePreference: () => void;
  unlockTabBarScrollHide: () => void;
  syncDeviceLocale: () => void;
};

type AppStore = AppState & AppActions;

export const appI18nStore = createStore<AppStore>()(
  persist(
    (set) => ({
      isTabBarScrollLocked: true,
      localeOverride: null,
      resolvedLocale: detectDeviceLocale(),
      themePreference: "light",
      setLocaleOverride: (locale) =>
        set({
          localeOverride: locale,
          resolvedLocale: locale,
        }),
      clearLocaleOverride: () =>
        set({
          localeOverride: null,
          resolvedLocale: detectDeviceLocale(),
        }),
      lockTabBarScrollHide: () =>
        set({
          isTabBarScrollLocked: true,
        }),
      setThemePreference: (themePreference) =>
        set({
          themePreference,
        }),
      toggleThemePreference: () =>
        set((state) => ({
          themePreference: state.themePreference === "light" ? "dark" : "light",
        })),
      syncDeviceLocale: () =>
        set((state) => {
          if (state.localeOverride) {
            return state;
          }

          return {
            resolvedLocale: detectDeviceLocale(),
          };
        }),
      unlockTabBarScrollHide: () =>
        set({
          isTabBarScrollLocked: false,
        }),
    }),
    {
      name: "app-store",
      partialize: (state) => ({
        localeOverride: state.localeOverride,
        resolvedLocale: state.localeOverride ?? state.resolvedLocale,
        themePreference: state.themePreference,
      }),
      storage: createJSONStorage(() => mmkvStateStorage),
    },
  ),
);

export function useAppStore<T>(selector: (state: AppStore) => T) {
  return useStore(appI18nStore, selector);
}
</file>

<file path="src/theme/tokens.ts">
import { APP_TEXT_FONT_FAMILY } from '@/core/fonts/app-fonts';

export const colors = {
  brand: {
50: '#F2F7F4',
100: '#D9EADF',
200: '#B2D5BE',
300: '#84B898',
400: '#5E9E76',
500: '#3F7F5A',
600: '#2C6546',
700: '#214F37',
800: '#173627',
900: '#0E2017',
  },
  neutral: {
0: '#FFFFFF',
50: '#F6F7F8',
100: '#E8EAED',
200: '#D6DADE',
300: '#B7BEC6',
400: '#8C96A0',
500: '#66717D',
600: '#4A5561',
700: '#35404C',
800: '#232C35',
900: '#12181E',
  },
app: {
canvas: '#F8F9FA',
text: '#1F2937',
textMuted: '#6B7280',
midnight: '#0F172A',
emerald: '#10B981',
surface: '#FFFFFF',
borderSubtle: '#E5E7EB',
},
success: '#2E8B57',
warning: '#D98E04',
danger: '#C44444',
info: '#2563EB',
} as const;

export const spacing = {
  none: 0,
  xxxs: 2,
  xxs: 4,
  xs: 8,
  sm: 12,
  md: 16,
  lg: 20,
  xl: 24,
  xxl: 32,
  xxxl: 40,
} as const;

export const radius = {
  none: 0,
  sm: 8,
  md: 12,
  lg: 16,
  xl: 24,
  full: 999,
} as const;

export const typography = {
  fontFamily: {
regular: APP_TEXT_FONT_FAMILY,
medium: APP_TEXT_FONT_FAMILY,
semibold: APP_TEXT_FONT_FAMILY,
bold: APP_TEXT_FONT_FAMILY,
  },
  fontSize: {
    xs: 12,
    sm: 14,
    md: 16,
    lg: 18,
    xl: 22,
    xxl: 28,
    display: 36,
  },
  lineHeight: {
    xs: 16,
    sm: 20,
    md: 24,
    lg: 28,
    xl: 32,
    xxl: 36,
    display: 42,
  },
} as const;

export const shadows = {
  sm: {
shadowColor: '#12181E',
    shadowOpacity: 0.08,
    shadowRadius: 8,
    shadowOffset: {
      width: 0,
      height: 4,
    },
    elevation: 2,
  },
  md: {
shadowColor: '#12181E',
    shadowOpacity: 0.12,
    shadowRadius: 16,
    shadowOffset: {
      width: 0,
      height: 10,
    },
    elevation: 4,
  },
} as const;

const lightButton = {
chip: {
iconSize: { md: 16, sm: 14 } as const,
radius: radius.full,
variants: {
category: {
default: {
backgroundColor: colors.app.surface,
borderColor: colors.app.borderSubtle,
borderWidth: 1,
iconColor: colors.app.textMuted,
textColor: colors.app.text,
},
selected: {
backgroundColor: `${colors.app.midnight}12`,
borderColor: colors.app.midnight,
borderWidth: 1,
iconColor: colors.app.midnight,
textColor: colors.app.midnight,
},
},
filter: {
default: {
backgroundColor: colors.neutral[50],
borderColor: colors.app.borderSubtle,
borderWidth: 1,
iconColor: colors.app.textMuted,
textColor: colors.app.text,
},
selected: {
backgroundColor: colors.app.midnight,
borderColor: colors.app.midnight,
borderWidth: 1,
iconColor: colors.neutral[0],
textColor: colors.neutral[0],
},
},
tag: {
default: {
backgroundColor: colors.app.surface,
borderColor: colors.app.borderSubtle,
borderWidth: 1,
iconColor: colors.app.textMuted,
textColor: colors.app.textMuted,
},
selected: {
backgroundColor: colors.neutral[100],
borderColor: colors.neutral[300],
borderWidth: 1,
iconColor: colors.app.text,
textColor: colors.app.text,
},
},
},
},
common: {
disabledOpacity: 0.45,
pressedOpacity: 0.88,
},
cta: {
radius: radius.md,
sizes: {
lg: { gap: 10, iconSize: 22, minHeight: 52, paddingHorizontal: 20, paddingVertical: 12 },
md: { gap: 8, iconSize: 20, minHeight: 44, paddingHorizontal: 16, paddingVertical: 10 },
sm: { gap: 6, iconSize: 18, minHeight: 36, paddingHorizontal: 12, paddingVertical: 8 },
},
variants: {
destructive: {
backgroundColor: colors.danger,
borderColor: colors.danger,
borderWidth: 0,
iconColor: colors.neutral[0],
textColor: colors.neutral[0],
},
ghost: {
backgroundColor: 'transparent',
borderColor: 'transparent',
borderWidth: 0,
iconColor: colors.app.midnight,
textColor: colors.app.midnight,
},
positive: {
backgroundColor: colors.app.emerald,
borderColor: colors.app.emerald,
borderWidth: 0,
iconColor: colors.neutral[0],
textColor: colors.neutral[0],
},
primary: {
backgroundColor: colors.app.midnight,
borderColor: colors.app.midnight,
borderWidth: 0,
iconColor: colors.neutral[0],
textColor: colors.neutral[0],
},
secondary: {
backgroundColor: 'transparent',
borderColor: colors.app.midnight,
borderWidth: 1.5,
iconColor: colors.app.midnight,
textColor: colors.app.midnight,
},
surface: {
backgroundColor: colors.app.surface,
borderColor: colors.app.borderSubtle,
borderWidth: 1,
iconColor: colors.app.midnight,
textColor: colors.app.text,
},
},
},
disabled: {
backgroundColor: colors.neutral[200],
borderColor: 'transparent',
borderWidth: 0,
iconColor: colors.neutral[500],
textColor: colors.neutral[500],
},
icon: {
radius: radius.md,
selected: {
backgroundColor: `${colors.app.midnight}18`,
borderColor: colors.app.midnight,
borderWidth: 1.5,
iconColor: colors.app.midnight,
},
sizes: {
lg: { iconSize: 24, size: 52 },
md: { iconSize: 22, size: 44 },
sm: { iconSize: 18, size: 36 },
},
variants: {
destructive: {
backgroundColor: `${colors.danger}18`,
borderColor: 'transparent',
borderWidth: 0,
iconColor: colors.danger,
},
ghost: {
backgroundColor: 'transparent',
borderColor: 'transparent',
borderWidth: 0,
iconColor: colors.app.midnight,
},
primary: {
backgroundColor: colors.app.midnight,
borderColor: colors.app.midnight,
borderWidth: 0,
iconColor: colors.neutral[0],
},
surface: {
backgroundColor: colors.app.surface,
borderColor: colors.app.borderSubtle,
borderWidth: 1,
iconColor: colors.app.midnight,
},
},
},
tab: {
badge: {
backgroundColor: colors.danger,
},
radius: radius.md,
sizes: {
md: { iconSize: 20, minHeight: 40, paddingHorizontal: 14, paddingVertical: 8 },
sm: { iconSize: 18, minHeight: 34, paddingHorizontal: 10, paddingVertical: 6 },
},
variants: {
default: {
backgroundColor: 'transparent',
borderColor: 'transparent',
borderWidth: 0,
iconColor: colors.app.textMuted,
textColor: colors.app.textMuted,
},
selected: {
backgroundColor: colors.app.surface,
borderColor: colors.app.midnight,
borderWidth: 1.5,
iconColor: colors.app.midnight,
textColor: colors.app.midnight,
},
},
},
} as const;

const darkSemantic = {
canvas: '#0B1220',
text: '#F3F4F6',
textMuted: '#9CA3AF',
midnight: '#E2E8F0',
emerald: '#10B981',
surface: '#1E293B',
borderSubtle: '#334155',
} as const;

const darkButton = {
chip: {
iconSize: { md: 16, sm: 14 } as const,
radius: radius.full,
variants: {
category: {
default: {
backgroundColor: darkSemantic.surface,
borderColor: darkSemantic.borderSubtle,
borderWidth: 1,
iconColor: darkSemantic.textMuted,
textColor: darkSemantic.text,
},
selected: {
backgroundColor: `${darkSemantic.midnight}22`,
borderColor: darkSemantic.midnight,
borderWidth: 1,
iconColor: darkSemantic.midnight,
textColor: darkSemantic.midnight,
},
},
filter: {
default: {
backgroundColor: colors.neutral[800],
borderColor: darkSemantic.borderSubtle,
borderWidth: 1,
iconColor: darkSemantic.textMuted,
textColor: darkSemantic.text,
},
selected: {
backgroundColor: colors.neutral[0],
borderColor: colors.neutral[0],
borderWidth: 1,
iconColor: colors.neutral[900],
textColor: colors.neutral[900],
},
},
tag: {
default: {
backgroundColor: darkSemantic.surface,
borderColor: darkSemantic.borderSubtle,
borderWidth: 1,
iconColor: darkSemantic.textMuted,
textColor: darkSemantic.textMuted,
},
selected: {
backgroundColor: colors.neutral[700],
borderColor: colors.neutral[500],
borderWidth: 1,
iconColor: darkSemantic.text,
textColor: darkSemantic.text,
},
},
},
},
common: lightButton.common,
cta: {
radius: radius.md,
sizes: lightButton.cta.sizes,
variants: {
destructive: {
backgroundColor: colors.danger,
borderColor: colors.danger,
borderWidth: 0,
iconColor: colors.neutral[0],
textColor: colors.neutral[0],
},
ghost: {
backgroundColor: 'transparent',
borderColor: 'transparent',
borderWidth: 0,
iconColor: darkSemantic.midnight,
textColor: darkSemantic.midnight,
},
positive: {
backgroundColor: darkSemantic.emerald,
borderColor: darkSemantic.emerald,
borderWidth: 0,
iconColor: colors.neutral[0],
textColor: colors.neutral[0],
},
primary: {
backgroundColor: colors.neutral[0],
borderColor: colors.neutral[0],
borderWidth: 0,
iconColor: colors.neutral[900],
textColor: colors.neutral[900],
},
secondary: {
backgroundColor: 'transparent',
borderColor: darkSemantic.midnight,
borderWidth: 1.5,
iconColor: darkSemantic.midnight,
textColor: darkSemantic.midnight,
},
surface: {
backgroundColor: darkSemantic.surface,
borderColor: darkSemantic.borderSubtle,
borderWidth: 1,
iconColor: darkSemantic.midnight,
textColor: darkSemantic.text,
},
},
},
disabled: {
backgroundColor: colors.neutral[700],
borderColor: 'transparent',
borderWidth: 0,
iconColor: colors.neutral[400],
textColor: colors.neutral[400],
},
icon: {
radius: radius.md,
selected: {
backgroundColor: `${colors.neutral[0]}22`,
borderColor: darkSemantic.midnight,
borderWidth: 1.5,
iconColor: darkSemantic.midnight,
},
sizes: lightButton.icon.sizes,
variants: {
destructive: {
backgroundColor: `${colors.danger}33`,
borderColor: 'transparent',
borderWidth: 0,
iconColor: '#FCA5A5',
},
ghost: {
backgroundColor: 'transparent',
borderColor: 'transparent',
borderWidth: 0,
iconColor: darkSemantic.midnight,
},
primary: {
backgroundColor: colors.neutral[0],
borderColor: colors.neutral[0],
borderWidth: 0,
iconColor: colors.neutral[900],
},
surface: {
backgroundColor: darkSemantic.surface,
borderColor: darkSemantic.borderSubtle,
borderWidth: 1,
iconColor: darkSemantic.midnight,
},
},
},
tab: {
badge: {
backgroundColor: colors.danger,
},
radius: radius.md,
sizes: lightButton.tab.sizes,
variants: {
default: {
backgroundColor: 'transparent',
borderColor: 'transparent',
borderWidth: 0,
iconColor: darkSemantic.textMuted,
textColor: darkSemantic.textMuted,
},
selected: {
backgroundColor: darkSemantic.surface,
borderColor: darkSemantic.midnight,
borderWidth: 1.5,
iconColor: darkSemantic.midnight,
textColor: darkSemantic.midnight,
},
},
},
} as const;

export const lightTheme = {
id: 'light',
button: lightButton,
  colors: {
accent: `${colors.app.midnight}14`,
background: colors.app.canvas,
border: colors.app.borderSubtle,
danger: colors.danger,
dangerForeground: colors.neutral[0],
info: colors.info,
positive: colors.app.emerald,
positiveForeground: colors.neutral[0],
primary: colors.app.midnight,
    primaryForeground: colors.neutral[0],
surface: colors.app.surface,
surfaceMuted: colors.neutral[50],
    success: colors.success,
text: colors.app.text,
textMuted: colors.app.textMuted,
    warning: colors.warning,
  },
radius,
shadows,
  spacing,
  typography,
} as const;

export const darkTheme = {
id: 'dark',
button: darkButton,
  colors: {
accent: `${darkSemantic.midnight}22`,
background: darkSemantic.canvas,
border: darkSemantic.borderSubtle,
danger: colors.danger,
dangerForeground: colors.neutral[0],
info: colors.info,
positive: darkSemantic.emerald,
positiveForeground: colors.neutral[0],
primary: darkSemantic.midnight,
    primaryForeground: colors.neutral[900],
surface: darkSemantic.surface,
surfaceMuted: colors.neutral[800],
    success: colors.success,
text: darkSemantic.text,
textMuted: darkSemantic.textMuted,
    warning: colors.warning,
  },
  radius,
shadows,
spacing,
  typography,
} as const;

export type AppTheme = typeof lightTheme | typeof darkTheme;
export type ThemePreference = AppTheme['id'];
</file>

<file path=".gitignore">
# Learn more https://docs.github.com/en/get-started/getting-started-with-git/ignoring-files

# dependencies
node_modules/

# Expo
.expo/
dist/
web-build/
expo-env.d.ts

# Native
.kotlin/
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# Metro
.metro-health-check*

# debug
npm-debug.*
yarn-debug.*
yarn-error.*

# macOS
.DS_Store
*.pem

# local env files
.env*.local

# typescript
*.tsbuildinfo

app-example

# generated native folders
/ios
/android

repomix-output.xml
</file>

<file path="eslint.config.js">
// https://docs.expo.dev/guides/using-eslint/
const { defineConfig } = require("eslint/config");
const expoConfig = require("eslint-config-expo/flat");
const eslintPluginPrettierRecommended = require("eslint-plugin-prettier/recommended");

module.exports = defineConfig([
eslintPluginPrettierRecommended,
expoConfig,
{
ignores: ["dist/*", "app-example/**"],
files: ["babel.config.js"],
},
]);
</file>

<file path="metro.config.js">
const { getDefaultConfig } = require("expo/metro-config");

const config = getDefaultConfig(__dirname);

config.transformer = {
...config.transformer,
babelTransformerPath: require.resolve("react-native-svg-transformer/expo"),
};

config.resolver = {
...config.resolver,
assetExts: config.resolver.assetExts.filter((ext) => ext !== "svg"),
sourceExts: [...config.resolver.sourceExts, "svg"],
};

module.exports = config;
</file>

<file path="README.md">
# ae-mobil

Expo tabanli tekrar kullanilabilir mobil uygulama altyapisi.

## Hedef

Bu repo bir demo ekranindan cok, farkli projelerde yeniden kullanabilecegimiz saglam bir temel olusturmak icin kuruldu.

Odak noktalarimiz:

- Katmanli ve anlasilir mimari
- Theme token tabanli UI sistemi
- Shared component kutuphanesi
- Expo Router ile net navigasyon yapisi
- i18n temelli dil kurgusu
- API katmani icin `axios` + `@tanstack/react-query`
- Global state icin `zustand`
- Feature-first genisleyebilen klasor yapisi

## Mimari V1

```text
app/ Expo Router route dosyalari
src/
components/ Ortak UI primitive ve composite componentler
core/ Provider, config, constants, bootstrap
features/ Feature bazli ekran ve moduller
i18n/ Dil altyapisi ve translation kaynaklari
services/ API istemcisi ve remote data yardimcilari
store/ Global store kurallari ve store'lar
theme/ Tokens, theme contract, helper'lar
```

## Simdiki Durum

Ilk adim olarak temel iskelet ve ilk entegrasyonlar kuruldu:

- `src` tabanli dizin yapisi
- Theme token sistemi
- Shared `AppScreen`, `AppText`, `AppButton`, `SurfaceCard` componentleri
- `i18next` tabanli locale-aware i18n yapisi
- `react-query` provider katmani
- `axios` tabanli API istemcisi
- `zustand` tabanli global app store

## Sonraki Paketler

Kurulan cekirdek paketler:

- `@tanstack/react-query`
- `axios`
- `zustand`
- `i18next`
- `react-i18next`
- `expo-localization`

Ikinci asama adaylari:

- `zod`
- `react-hook-form`

## Komutlar

```bash
npm run start
npm run ios
npm run android
npm run web
npm run icons:generate
```

## Ikon Sistemi

Proje custom ikonlari `SVGR + react-native-svg` ile `assets/icons/svg` klasorundaki SVG dosyalarindan component olarak
kullanir.

- Kaynak SVG klasoru: `assets/icons/svg`
- Uretilen registry: `src/components/ui/app-icon.registry.ts`
- Uygulama ici kullanim: `AppIcon`
- Tek renkli ikon standardi: `fill="currentColor"` veya `stroke="currentColor"`

Yeni ikon ekleme akisi:

```bash
npm run icons:generate
```

Kullanim ornegi:

```tsx
<AppIcon name="bell-plus" size={24} color="#111827" />
```

`color` override yalnizca `currentColor` kullanan tek renkli SVG'lerde garantilidir. Cok renkli SVG'ler kendi renklerini
korur.

# expo-template
</file>

<file path="app/_layout.tsx">
import '@/theme/unistyles';
import { useFonts } from 'expo-font';
import { SplashScreen, Stack } from 'expo-router';
import { useEffect } from 'react';

import { appFonts } from '@/core/fonts/app-fonts';
import { AppProviders } from '@/core/providers/app-providers';
import { ThemeScope } from '@/core/providers/theme-scope';
import { useSession } from '@/features/auth';

void SplashScreen.preventAutoHideAsync();

export default function RootLayout() {
return (
<AppProviders>
    <ThemeScope>
        <RootNavigator/>
    </ThemeScope>
</AppProviders>
);
}

function RootNavigator() {
const [fontsLoaded, fontsError] = useFonts(appFonts);
const { bootstrap, hasCompletedOnboarding, isLoading } = useSession();

useEffect(() => {
bootstrap();
}, [bootstrap]);

useEffect(() => {
if ((fontsLoaded || fontsError) && !isLoading) {
void SplashScreen.hideAsync();
}
}, [fontsError, fontsLoaded, isLoading]);

if ((!fontsLoaded && !fontsError) || isLoading) {
return null;
}

return (
<Stack screenOptions={{ headerShown: false }}>
<Stack.Screen
        name="index"
        options={{
animation: 'none',
}}
/>

<Stack.Protected guard={!hasCompletedOnboarding}>
<Stack.Screen name="(onboarding)"/>
</Stack.Protected>

<Stack.Protected guard={hasCompletedOnboarding}>
<Stack.Screen name="(app)"/>
<Stack.Screen name="(auth)"/>
        </Stack.Protected>

<Stack.Screen
name="modal"
options={{
        headerShown: false,
        presentation: 'modal',
        }}
        />
        </Stack>
        );
        }
        </file>

<file path="src/features/app/screens/app-home-screen.tsx">
import { StyleSheet } from 'react-native-unistyles';
import { Map } from '@/components/map';
import { SafeAreaProvider, useSafeAreaInsets } from 'react-native-safe-area-context';

export function AppHomeScreen() {
const { top } = useSafeAreaInsets();
return (
<SafeAreaProvider style={[styles.container]}>
<Map/>
</SafeAreaProvider>
        );
        }

        const styles = StyleSheet.create({
        container: {
        flex: 1,
        },
        });
</file>

<file path="src/features/auth/store/session-store.ts">
import { createStore, useStore } from 'zustand';
import { createJSONStorage, persist } from 'zustand/middleware';

import {
clearAuthTokens,
  getAccessToken,
  setAccessToken,
setRefreshToken,
} from '@/services/storage/secure-storage';
import { mmkvStateStorage } from '@/store/storage';

type SessionStatus = 'checking' | 'authenticated' | 'guest';

type SessionState = {
  hasCompletedOnboarding: boolean;
  status: SessionStatus;
  sessionToken: string | null;
};

type SessionActions = {
  bootstrap: () => Promise<void>;
  completeOnboarding: () => void;
  register: () => Promise<void>;
  signIn: () => Promise<void>;
  signOut: () => Promise<void>;
/** API’den dönen gerçek token çifti */
setSessionTokens: (accessToken: string, refreshToken: string) => Promise<void>;
};

type SessionStore = SessionState & SessionActions;

export const sessionStore = createStore<SessionStore>()(
  persist(
    (set, get) => ({
      hasCompletedOnboarding: false,
status: 'checking',
      sessionToken: null,
      bootstrap: async () => {
        const token = await getAccessToken();

        set({
          sessionToken: token,
status: token ? 'authenticated' : 'guest',
        });
      },
      completeOnboarding: () =>
        set({
          hasCompletedOnboarding: true,
        }),
      register: async () => {
const sessionToken = 'demo-session-new-user';
        await setAccessToken(sessionToken);

        set({
          hasCompletedOnboarding: false,
status: 'authenticated',
          sessionToken,
        });
      },
      signIn: async () => {
const sessionToken = 'demo-session-existing-user';
        await setAccessToken(sessionToken);

        set({
          hasCompletedOnboarding: true,
status: 'authenticated',
          sessionToken,
        });
      },
      signOut: async () => {
await clearAuthTokens();

        set({
status: 'guest',
          sessionToken: null,
        });
      },
setSessionTokens: async (accessToken, refreshToken) => {
await setAccessToken(accessToken);
await setRefreshToken(refreshToken);
set({
status: 'authenticated',
sessionToken: accessToken,
});
},
    }),
    {
name: 'session-store',
      partialize: (state) => ({
        hasCompletedOnboarding: state.hasCompletedOnboarding,
      }),
      storage: createJSONStorage(() => mmkvStateStorage),
    },
  ),
);

export function useSessionStore<T>(selector: (state: SessionStore) => T) {
  return useStore(sessionStore, selector);
}
</file>

<file path="app.json">
{
  "expo": {
    "name": "ae-mobil",
    "slug": "ae-mobil",
    "version": "1.0.0",
    "orientation": "portrait",
    "icon": "./assets/images/icon.png",
    "scheme": "aemobil",
    "userInterfaceStyle": "automatic",
"ios": {
"infoPlist": {
"ExpoLocalization_supportsRTL": true,
"CFBundleLocalizations": ["en", "tr", "ar"],
"NSLocationWhenInUseUsageDescription": "Konumunuz haritada gösterilir ve size yakın ilanlar sunulur.",
"NSLocationAlwaysAndWhenInUseUsageDescription": "Konumunuz haritada gösterilir ve size yakın ilanlar sunulur."
},
"bundleIdentifier": "com.eoist.ae",
"config": {
"usesNonExemptEncryption": false
}
},
    "android": {
      "adaptiveIcon": {
        "backgroundColor": "#E6F4FE",
        "foregroundImage": "./assets/images/android-icon-foreground.png",
        "backgroundImage": "./assets/images/android-icon-background.png",
        "monochromeImage": "./assets/images/android-icon-monochrome.png"
      },
      "predictiveBackGestureEnabled": false,
"package": "com.eoist.ae"
    },
    "web": {
      "bundler": "metro",
      "output": "static",
      "favicon": "./assets/images/favicon.png"
    },
    "extra": {
      "supportsRTL": true
    },
"locales": {
"en": "./locales/native/en.json",
"tr": "./locales/native/tr.json",
"ar": "./locales/native/ar.json"
},
    "plugins": [
      "expo-router",
      [
        "expo-localization",
        {
          "supportedLocales": {
"ios": ["en", "tr", "ar"],
"android": ["en", "tr", "ar"]
          }
        }
      ],
      [
        "expo-splash-screen",
        {
          "image": "./assets/images/splash-icon.png",
          "imageWidth": 200,
          "resizeMode": "contain",
          "backgroundColor": "#ffffff",
          "dark": {
            "backgroundColor": "#000000"
          }
        }
      ],
      [
        "expo-secure-store",
        {
          "configureAndroidBackup": true
        }
      ],
[
"expo-font",
{
"fonts": ["./assets/fonts/Figtree-VariableFont_wght.ttf"]
}
],
[
"expo-location",
{
"locationWhenInUsePermission": "Konumunuz haritada gösterilir ve size yakın ilanlar sunulur.",
"locationAlwaysAndWhenInUsePermission": "Konumunuz haritada gösterilir ve size yakın ilanlar sunulur."
}
],
      "expo-image",
      "expo-web-browser"
]
  },
  "experiments": {
    "typedRoutes": true,
    "reactCompiler": true
}
}
</file>

<file path="tsconfig.json">
{
"extends": "expo/tsconfig.base",
"compilerOptions": {
"jsx": "react-jsx",
"baseUrl": ".",
"strict": true,
"paths": {
"@/*": [
"./src/*"
      ]
}
},
"include": [
"**/*.ts",
"**/*.tsx"
],
"exclude": [
"app-example"
]
}
</file>

<file path="package.json">
{
"name": "ae-mobil",
  "main": "index.js",
  "version": "1.0.0",
  "scripts": {
    "start": "expo start",
    "android": "expo run:android",
    "android:debug": "expo run:android --mode debug",
    "android:release": "expo run:android --mode release",
    "ios": "expo run:ios",
    "ios:debug": "expo run:ios --mode debug",
    "ios:release": "expo run:ios --mode release",
    "web": "expo start --web",
    "lint": "expo lint",
"icons:generate": "node ./scripts/generate-svg-icons.mjs",
"repomix": "npx repomix --ignore 'android/**,ios/**,assets/**,src/i18n/translations/**,.agents/skills/**'"
  },
  "dependencies": {
    "@expo/vector-icons": "^15.0.3",
"@gorhom/bottom-sheet": "^5.2.13",
    "@react-navigation/bottom-tabs": "^7.4.0",
    "@react-navigation/elements": "^2.6.3",
    "@react-navigation/native": "^7.1.8",
    "@tanstack/react-query": "^5.99.0",
    "axios": "^1.15.0",
"expo": "~55.0.19",
"expo-constants": "~55.0.15",
    "expo-font": "~55.0.6",
    "expo-haptics": "~55.0.14",
"expo-image": "~55.0.9",
"expo-intent-launcher": "~55.0.12",
"expo-linking": "~55.0.14",
    "expo-localization": "^55.0.13",
"expo-location": "~55.1.8",
"expo-router": "~55.0.13",
    "expo-secure-store": "~55.0.13",
"expo-splash-screen": "~55.0.19",
    "expo-status-bar": "~55.0.5",
    "expo-symbols": "~55.0.7",
"expo-system-ui": "~55.0.16",
    "expo-web-browser": "~55.0.14",
    "i18next": "^26.0.5",
    "react": "19.2.0",
    "react-dom": "19.2.0",
    "react-i18next": "^17.0.4",
"react-native": "0.83.6",
    "react-native-gesture-handler": "~2.30.0",
"react-native-maps": "1.27.2",
    "react-native-mmkv": "^4.3.1",
    "react-native-nitro-modules": "^0.35.4",
    "react-native-reanimated": "4.2.1",
    "react-native-safe-area-context": "~5.6.0",
    "react-native-screens": "~4.23.0",
    "react-native-svg": "15.15.3",
    "react-native-unistyles": "^3.2.3",
    "react-native-web": "~0.21.0",
"react-native-worklets": "0.7.4",
    "zustand": "^5.0.12",
    "zustand-mmkv-storage": "^1.0.0"
  },
  "devDependencies": {
    "@types/react": "~19.2.10",
    "babel-preset-expo": "~55.0.8",
    "eslint": "^9.25.0",
    "eslint-config-expo": "~55.0.0",
    "eslint-config-prettier": "^10.1.8",
    "eslint-plugin-prettier": "^5.5.5",
    "prettier": "^3.8.3",
    "react-native-svg-transformer": "latest",
    "typescript": "~5.9.2"
  },
  "private": true
}
</file>

</files>
